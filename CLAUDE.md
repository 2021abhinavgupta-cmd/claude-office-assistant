# CLAUDE.md — AI Coding Assistant Guide

This file provides essential context for AI coding assistants (System, Copilot, etc.) working on this codebase.

** Token Saving Tip for Users:** When starting a new chat, you can simply say *"Review CLAUDE.md to understand the architecture, then [do my task]"*. This gives the AI all the routing, database, and UI patterns immediately without having to burn context tokens manually scanning the repository files.

---

## Project Overview

**Agency Portal Assistant** is a full-stack, production-deployed internal tool for a small team (8 employees). It is a System AI-powered workspace that combines:

- Multi-turn intelligent chat workspace (auto-routed by task complexity)
- Project & client management with per-project knowledge bases
- Daily standups, task tracking, and attendance
- Live "Huddle" chat (multi-user real-time via Server-Sent Events)
- Smart auto-tagging of conversations by project/client
- A client portal for external clients to log in and view their tasks
- Notion integration, document export, HTML generator, and presentation builder

**Deployment:** Railway (persistent volume at `/logs/` for the SQLite DB).  
**Live URL:** The app is accessed via the Railway-assigned URL.

---

## Tech Stack

| Layer | Technology |
|---|---|
| Backend | Python 3.11, Flask, Gunicorn (gevent worker) |
| AI | Anthropic Python SDK (Standard/Fast models) |
| Frontend | Vanilla HTML/CSS/JS (zero build step) |
| Database | SQLite via `logs/app.db` (WAL mode) |
| Scheduling | APScheduler (`task_scheduler.py` — daily cron) |
| Rate limiting | Flask-Limiter (see `requirements.txt`) |
| Deployment | Railway with Nixpacks |
| Auth | Server-side session tokens (stored in SQLite `sessions` table) |

**Notable deps** (`backend/requirements.txt`): `anthropic`, `flask-cors`, `flask-compress`, `Flask-Limiter`, `apscheduler`, `gevent`, `pypdf`/`python-docx`/`openpyxl` (file parsing), `weasyprint`/`python-pptx`/`reportlab` (export), `google-auth` (Google Sheets two-way sync, see gotcha #87). Notion integration and Google Sheets sync both use raw `requests`/`AuthorizedSession`, not their respective official SDKs.

---

## Directory Structure

```
claude-office-assistant/
├── backend/
│   ├── app.py                  # Main Flask app — ALL conversation + AI routes
│   ├── db.py                   # SQLite init, schema, migrations. Run on startup.
│   ├── conversation_store.py   # CRUD for conversations (SQLite-backed)
│   ├── memory_store.py         # Per-user AI memory (persistent profiles)
│   ├── project_store.py        # Project & knowledge base CRUD
│   ├── model_router.py         # Task-to-model routing logic (haiku vs sonnet)
│   ├── system_prompt.py        # Master system prompt + per-task prompt builder
│   ├── budget_tracker.py       # Monthly API cost tracking + budget enforcement
│   ├── file_processor.py       # PDF/CSV/Word file parsing for attachments
│   ├── kb_retriever.py         # FTS5 knowledge base retrieval (System Projects-style)
│   ├── document_exporter.py    # Export chats to DOCX/PDF
│   ├── notifications.py        # WhatsApp notifications via Twilio (outbound)
│   ├── skills.py               # Builtin skill library (8 hardcoded skills)
│   ├── custom_skills_store.py  # CRUD for user-created custom skills (SQLite)
│   ├── notion_store.py         # Full Notion API integration
│   ├── task_scheduler.py       # Background task risk escalation (APScheduler, daily 08:00)
│   ├── web_fetcher.py          # SSRF-guarded URL scraper — powers /api/fetch-url + inline link fetching in chat
│   ├── google_sheets_store.py  # Google Sheets two-way sync — raw Sheets API v4 client, field mapping, push/reconcile (see gotcha #87)
│   ├── utils.py                # IST timezone helpers, atomic employees.json read/write, its own _is_admin()
│   ├── extensions.py           # Shared Flask app instance (avoids circular imports)
│   └── routes/
│       ├── __init__.py         # Empty — blueprint registration actually happens in app.py, not here
│       ├── auth.py             # Login, logout, session verify, employee list
│       ├── attendance.py       # Check-in/checkout, daily attendance records
│       ├── ops.py              # Standups, tasks, alerts, SQLite admin, exports, bet, discovery forms
│       ├── system.py           # Health check, budget admin, usage stats
│       └── sheets_sync.py      # Google Sheets link management + push/pull-webhook routes (see gotcha #87)
├── frontend/
│   ├── index.html              # MAIN CHAT UI (app entry point)
│   ├── app.js                  # All frontend logic for index.html (2200+ lines)
│   ├── kanban.js                # MISLEADING NAME: actually the Projects/KB side-panel grid for index.html — NOT a kanban board (see Gotchas)
│   ├── optimizer.js            # Prompt Optimizer panel, extracted out of app.js
│   ├── skills.js                # Builtin/custom skills menu + manager modal, extracted out of app.js
│   ├── utils.js                 # index.html-only: markdown/text rendering, code-block copy/download/preview buttons
│   ├── style.css               # All styles (dark mode, glassmorphism)
│   ├── auth.js                 # Shared auth guard (include on every protected page)
│   ├── shared-config.js        # Shared employee loader (window.EMPLOYEES)
│   ├── login.html              # Employee PIN login
│   ├── dashboard.html          # Founder analytics dashboard
│   ├── api-costing.html        # Per-model cost/usage dashboard (split out of dashboard.html)
│   ├── projects.html           # Projects & knowledge base manager
│   ├── standup.html            # Daily standup submission + team view
│   ├── my-tasks.html           # Personal task tracker with subtasks/delegation
│   ├── project.html            # Individual project page
│   ├── project.js              # JS for project.html
│   ├── presentation.html       # AI presentation builder
│   ├── html-generator.html     # AI HTML page generator
│   ├── discovery-form.html     # PUBLIC, unauthenticated client discovery questionnaire
│   ├── manage-forms.html       # Admin editor for the discovery form schema (form_templates table)
│   ├── client-forms-db.html    # Admin viewer for submitted discovery answers (discovery_submissions table)
│   ├── bet.html                 # Internal joke feature: lunch bet poll, allowlisted to 4 employee IDs
│   ├── client-login.html       # Client portal login (separate from employee login)
│   ├── client-dashboard.html   # Client portal dashboard
│   ├── client-portal.html      # Client task view
│   ├── client-admin.html       # Admin: manage client accounts
│   ├── client-auth.js          # Auth guard for client portal pages
│   ├── client-onboard.html     # New client onboarding form (3 steps: Info → Services → Review & Create). Includes Username/Password fields for client portal access. Success screen offers Done or  Add Tasks
│   └── add-tasks.html          # Apply workflow task templates to an existing client (select client → pick workflow(s) → POST to /api/clients/<id>/auto-tasks)
├── config/
│   └── employees.json          # Static employee data (id, name, pin, role, etc.)
├── logs/                       # Persistent volume — DO NOT delete
│   └── app.db                  # SQLite database (all production data lives here — the ONLY real DB file)
├── scripts/                    # Utility/migration scripts
├── requirements.txt            # Python dependencies
├── Procfile                    # Railway: web: gunicorn backend.app:app
├── nixpacks.toml               # Railway build config
└── railway.toml                # Railway deploy config
```

> [!NOTE]
> **Repo-root clutter — cleaned up 2026-07-20 (commit follows this doc update).** All the one-off `fix_*.py` / `test_*.py` / `patch.py` / `query_db.py` scripts and stray `.db` files (`app_old.db`, `backend/local.db`, `backend/database.db`, etc.) that used to accumulate at the repo root and in `backend/` from past AI-assisted debugging sessions have been deleted (none were ever imported by the app or referenced anywhere in `backend/`/`frontend/` — verified by grep before deletion). `.gitignore` now also blocks `/fix_*.py`, `/test_*.py`, `/patch.py`, `/query_db.py`, `/scratch.py`, `/*_edit.py`, and root/`backend`-level `*.db` files from being re-added by mistake. **The only DB the app ever reads/writes is `logs/app.db`** via `db.py::get_connection()` — that file is untouched and still the sole source of truth.

---

## Key Patterns & Conventions

### 1. Authentication (Employee)
- Login is PIN-based (4-digit PIN in `config/employees.json`)
- `auth.py` issues a session token stored in the `sessions` table
- Every protected page includes `<script src="auth.js"></script>` which calls `/api/auth/verify` and redirects to `login.html` if invalid
- The `currentUser` object (`{user_id, user_name, role, is_admin}`) is stored in `localStorage` under `"agency_portal_user"`

### 2. Authentication (Client Portal)
- Clients log in at `client-login.html` using username + password
- Stored in `client_users` table. Separate `client_sessions` table.
- Client pages include `client-auth.js` (NOT `auth.js`)

> [!WARNING]
> **Known Security Flaws in Client Onboarding:**
> 1. **Plaintext Passwords — FIXED, this warning is stale.** `routes/auth.py::client_login()` now hashes new passwords with `werkzeug.security.generate_password_hash()` (`create_client_user()`, `update_client_user()`) and seamlessly upgrades any legacy plaintext row to a hash on next successful login. Don't re-flag this without checking the code first.
> 2. **`POST /api/clients` (`app.py::create_client`)** actually authenticates via session cookie (`_verify_session`), not body spoofing — this warning was stale. (It *was* broken a different way until this session: missing `body = request.get_json(...)` meant every call 500'd unconditionally — now fixed.)
> 3. **Missing Auth on Notion endpoint:** `POST /api/notion/clients` (`routes/ops.py`) has completely missing authentication/authorization. Anyone can create a Notion client.
> 4. **Bet feature spoofing:** `POST /api/bet` and `POST /api/bet/question` (`routes/ops.py`) authorize by checking a client-supplied `user_id` against a hardcoded whitelist (`["emp002","emp003","emp007","emp008"]`) — zero session verification. Same spoofing pattern as #2, just lower stakes.
> 5. **Discovery form fully public, no rate limit:** `GET/POST /api/form-templates*` and `POST /api/discovery-submissions` (`routes/ops.py`) have no auth check at all. `discovery-form.html` is intentionally public (it's a client-facing intake form), but the submission endpoint can be spammed by anyone with the link.
> 6. **~~Inconsistent admin allowlists~~ — RESOLVED 2026-07-20, by design change, not a bug fix.** `utils.py::_is_admin()` and `app.py`'s own `_is_admin()` both now just `return bool(user_id)` — every logged-in employee has admin access (user request: "everyone should get complete access"). The `ONBOARD_ADMINS` arrays in `add-tasks.html`/`client-onboard.html` were removed (gate is now just "is logged in"); `dashboard.html`'s `ONBOARD_ADMINS` was left as-is (still gates visibility of Onboard/Backup DB buttons but no longer matches backend reality — harmless since backend re-checks). The **bet** feature (`["emp002","emp003","emp007","emp008"]` = Nupur/Abhinav/Palak/Happy) is the one deliberate exception and was explicitly left untouched. **Caveat:** `routes/attendance.py::attendance_logs()`/`attendance_export()` read `user_id` from an unauthenticated query string, not a session — `bool(user_id)` means *any* non-empty string now passes, not just real employee IDs. Flagged to user, not yet fixed (would need session-token verification instead of query-param admin_id).
> 7. **`GET/POST/DELETE /api/auth/clients[/<id>]` have zero auth — only `PUT` checks it.** (`routes/auth.py`) Someone added `_verify_admin_session()` to `update_client_user()` (PUT, ~line 466) but never added it to `list_client_users()` (GET, ~319), `create_client_user()` (POST, ~335), or `delete_client_user()` (DELETE, ~367). As-is, anyone can list every client portal account (username/client_name/notion_id), create a new one for any client, or delete any existing one — no cookie or token required. Made worse by `CORS(app, resources={r"/api/*": {"origins": "*"}})` (`app.py:80`): the unauthenticated GET is scrapable by background JS from any website, not just via direct URL. **Flagged, not fixed** — user explicitly deferred this fix in-session (2026-07-18) pending verification that `client-admin.html` actually sends the admin cookie on these calls before locking them down. Fix by mirroring the PUT handler's `_verify_admin_session()` check onto the other three.
> 8. **No rate limiting on any login endpoint.** `extensions.py` sets `Limiter(default_limits=[])`; only two unrelated routes in `app.py` have an explicit `@limiter.limit(...)`. Employee login is a 4-digit PIN checked by plain string equality with no lockout — brute-forceable (10,000 combinations). `POST /api/auth/change_pin` also trusts a body-supplied `user_id` with no session check (only `old_pin` gates it), so a brute-forced PIN plus this endpoint is a full account-takeover path. **Flagged, not fixed** — same deferral as #7.

### 3. AI Routing
- Task types: `general`, `coding`, `html_design`, `presentations`, `email`, `scripts`, `captions`, `meetings`, `announcements`, `analysis`, `data_analysis`, `content`, `whatsapp`
- `model_router.py` maps task type → model tier (haiku/sonnet). `whatsapp` maps to Haiku.
- The streaming endpoint `POST /api/conversations/<id>/stream` is the primary chat endpoint
- If `should_think()` returns True (complex query), the model is pinned to `claude-sonnet-4-6` with extended thinking enabled (`budget_tokens=14000`)
- Optional request body fields for the stream endpoint:
  - `skill_id` — builtin skill key (e.g. `"web_search"`) or custom skill ID prefixed `sk_`
  - `style` — `"concise"`, `"detailed"`, or `"formal"`; injects a style instruction into the system prompt
  - `webSearchEnabled` — boolean; enables web search tool (also auto-enabled when `skill_id == "web_search"`)

### 4. Conversation & Huddle System
- Conversations are stored as JSON blobs in the `conversations` SQLite table
- Each conversation has: `id`, `user_id`, `title`, `messages[]`, `task_type`, `project_id`, `participant_ids[]`, `participant_names{}`
- **Huddle** = a conversation with `len(participant_ids) > 1`
- Live SSE stream for huddles: `GET /api/conversations/<id>/huddle-events`
- Invite endpoint: `POST /api/conversations/<id>/invite` — body: `{user_id, user_name}`
- The frontend polls `loadConversations()` every 5 seconds so invited users see the new chat without refreshing

### 5. Smart Auto-Tagging
- When the **first message** of a new conversation is sent, `_auto_tag_bg()` runs in a background thread
- It calls System Haiku with a list of all projects/clients, asks which one matches the message
- If a match is found, it updates the conversation's `project_id` or `client_id` in the DB
- The frontend re-renders the tag badge automatically via the SSE `done` event

### 6. Database
- Single SQLite file at `logs/app.db`
- `db.py` calls `init_db()` and `migrate_from_json()` on every startup (safe/idempotent)
- All `ALTER TABLE ... ADD COLUMN` are wrapped in `try/except` to be non-breaking
- Always use `get_connection()` from `db.py` — never create `sqlite3.connect()` directly
- FTS5 Virtual Table for the RAG knowledge base is actually named **`kb_chunks_fts`** (columns: `project_id`, `user_id`, `doc_id`, `filename`, `chunk`; `tokenize='porter'`, ranked with `bm25()`). It is *not* called `project_knowledge` despite that name appearing in older comments/docs — don't search for a `project_knowledge` table, it doesn't exist.
- **Full table list** (`init_db()` in `db.py`): `budget`, `conversations`, `memory`, `custom_skills`, `usage_logs`, `attendance`, `daily_attendance`, `standups`, `mohit_bets` (bet feature), `app_settings` (key/value, e.g. `bet_question`), `standup_tasks`, `task_risk` (scheduler escalation), `sessions`, `client_users`, `client_dependencies`, `client_sessions`, `client_task_feedback`, `projects`, `form_templates` (discovery form schema), `client_form_answers` (per-client answers — has API endpoints but **no dedicated frontend page**, looks half-built), `discovery_submissions` (public intake submissions), `project_files`, `kb_chunks_fts`, `sheet_edit_log` (Sheets version history — see gotcha #74), `sheet_hidden_rows` (shared Sheets row-hide state — see gotcha #83), `google_sheet_links` (Google Sheets two-way sync link per client — see gotcha #87).

### 7. Skills System
- Two tiers: **builtin** (defined in `skills.py`) and **custom** (stored in `custom_skills` table, IDs prefixed `sk_`).
- Builtin skills: `web_search`, `content_writer`, `email_drafter`, `video_scripter`, `meeting_summary`, `data_analyst`, `code_helper`, `social_caption`.
- Skills inject a `"SKILL INSTRUCTION:\n"` block prepended to the system prompt during streaming.
- If a skill specifies `"model": "sonnet"` and the user hasn't set a `model_override`, the request auto-upgrades to Sonnet.
- `web_search` builtin skill also forces `webSearchEnabled = True`.
- Custom skills are per-user but can be marked `is_shared=1` to appear for all users.
- API endpoints: `GET /api/skills?user_id=` → `{builtin, custom}`, `POST /api/skills/custom` → create, `DELETE /api/skills/custom/<id>` → delete (owner only).
- Frontend: Prompt Optimizer panel lives in `frontend/optimizer.js` and the skills menu/manager lives in `frontend/skills.js` — both were extracted out of `app.js` into their own files, both loaded only by `index.html`.

### 8. FTS5 Knowledge Base & File Processing (RAG)
- Projects can have uploaded documents (PDF, DOCX, CSV, TXT, MD, etc.).
- `file_processor.py` parses these documents on upload, extracting raw text.
- Text is inserted into the `kb_chunks_fts` SQLite FTS5 virtual table for fast full-text search.
- `kb_retriever.py` intercepts queries. When auto-tagging associates a chat with a specific `project_id`, the system performs a BM25 keyword search against `kb_chunks_fts` using the user's query.
- The top matched snippets are injected directly into the conversation context window so Claude can answer questions based on the client's uploaded files.

### 9. Persistent User Memory
- Managed by `memory_store.py`.
- Stores unstructured facts, profiles, or rules about a user in a JSON list format inside the `memory` SQLite table. Max 50 items per user.
- Every chat stream explicitly calls `format_for_prompt()` to extract the user's latest 20 memories. FTS is NOT used here; they are simply appended to the `MASTER_SYSTEM_PROMPT`.
- Also includes `format_team_memories()` which allows the AI to reference stylistic writing traits from *other* team members if asked to "write this in Sarah's style".

### 10. Frontend Architecture & DOM Updates
- **Zero-Build Vanilla JS**: The app relies entirely on standard ES6 JavaScript in `app.js` (no React, Vue, or bundler).
- **DOM Mutations**: State is managed manually. Functions like `appendMessage()`, `showWelcomeScreen()`, and `renderConversations()` physically detach and re-attach HTML elements. 
- Always ensure parent containers are visible (`display: flex` or `block`) before expecting appended elements to size correctly (e.g. scrollbars on `textarea`).
- **Base API URL**: Defined once at the top of `app.js`:
  ```js
  const API = location.hostname === 'localhost' ? 'http://localhost:5000' : location.origin;
  ```
- All other frontend files use the same pattern inline or via `shared-config.js`

### 11. Discovery Questionnaire System
- Three-part feature built around a dynamic, admin-editable form schema:
  1. **`manage-forms.html`** (employee-only) edits the question schema via `GET/POST /api/form-templates[/<id>]`, stored as JSON in the `form_templates` table (default template id `discovery_global`, seeded in `db.py` with 32 questions across 10 sections).
  2. **`discovery-form.html`** is the **public, unauthenticated** questionnaire — no `auth.js`/`client-auth.js` include. It fetches the schema via `GET /api/form-templates/<form_id>` and submits via `POST /api/discovery-submissions` with `{form_id, company_name, email, answers}`.
  3. **`client-forms-db.html`** (employee-only) lists all submissions via `GET /api/discovery-submissions`.
- A separate, similarly-named table `client_form_answers` (per-client, keyed by `client_id`) used to have its own endpoints (`GET/POST /api/clients/<id>/form-answers`) — **removed 2026-07-20** since no frontend page ever called them (confirmed by grep, half-finished parallel feature, not to be confused with `discovery_submissions`). The `client_form_answers` table creation in `db.py` was deliberately left in place (harmless, avoids a migration) but nothing writes to it anymore.

### 12. Task Risk Escalation (Scheduler)
- `task_scheduler.py::init_scheduler(app)` registers an APScheduler job that runs daily at **08:00**, plus fires once immediately on startup.
- It scans non-approved tasks (Notion or SQLite) with due dates and escalates `task_risk.risk_level` through `day1` (reminder) → `day2` (warning) → `day3` (at_risk) → `day5` (critical), tracked per-task via the `alerted_dayN` columns so each threshold only fires once.
- Fired alerts are written to `usage_logs` as `{"type": "task_alert", ...}` JSON blobs, which the founder dashboard reads to show recent risk alerts.

### 13. URL Fetching / SSRF Guard
- `backend/web_fetcher.py` powers `POST /api/fetch-url` and is also invoked inline during chat streaming when a user message contains a URL.
- `is_safe_url()` blocks requests to private/local IPs using `ipaddress.ip_address(...).is_global` before fetching — treat this as the canonical SSRF guard; any new URL-fetching feature should route through it rather than calling `requests.get()` directly.
- Fetched pages are parsed with BeautifulSoup (script/style/nav stripped) and truncated to 8000 chars before being injected into context.

---

## Environment Variables (Required)

| Variable | Description |
|---|---|
| `ANTHROPIC_API_KEY` | Anthropic API key (required) |
| `MONTHLY_BUDGET_LIMIT` | Max USD spend per month (e.g., `50`) |
| `SECRET_KEY` | Flask secret key for sessions |
| `TWILIO_ACCOUNT_SID` | (Optional) Outbound WhatsApp notifications via Twilio |
| `TWILIO_AUTH_TOKEN` | (Optional) Outbound WhatsApp notifications via Twilio |
| `TWILIO_WHATSAPP_FROM` | (Optional) Twilio WhatsApp sender number |
| `NOTION_TOKEN` | (Optional) Notion API integration |
| `WHATSAPP_PHONE_NUMBER_ID` | (Optional) Meta Cloud API phone number ID for WhatsApp bot |
| `WHATSAPP_ACCESS_TOKEN` | (Optional) Meta Cloud API bearer token for WhatsApp bot |
| `META_VERIFY_TOKEN` | (Optional) Shared secret for Meta webhook verification handshake |
| `GOOGLE_SHEETS_SERVICE_ACCOUNT_JSON` | (Optional) Raw JSON key of a Google Cloud service account with Sheets API access, powers two-way Google Sheets sync (see gotcha #87) |

---

## Running Locally

```bash
# 1. Install dependencies
pip install -r requirements.txt

# 2. Set environment variables in config/.env or shell
export ANTHROPIC_API_KEY=sk-ant-...

# 3. Start the backend
python -m backend.app
# OR
bash start.sh

# 4. Open the frontend
# Navigate to: http://localhost:5000/
```

The Flask app serves the `frontend/` folder as static files.

> [!NOTE]
> The default `python`/`pip` on PATH may not be this project's venv. Use `.venv/Scripts/python.exe` (Windows) explicitly for anything needing project deps (Flask, requests, etc) — a bare `python -m pyflakes ...` can silently run against an unrelated environment.

### Debugging
- `.venv/Scripts/python.exe -m pyflakes backend/*.py backend/routes/*.py` — static undefined-name check (`pip install pyflakes` first if missing). This codebase has a recurring copy-paste bug pattern: a variable set in one `if/else` branch, then referenced unconditionally afterward (or via a stray `for...else`) — silently 500s in production. Caught 4 real bugs this way in one session.

---

## Common Development Tasks

### Add a new API endpoint
Add it to `backend/app.py` (core conversation routes) or the appropriate Blueprint in `backend/routes/`:
- Auth-related → `routes/auth.py`
- Standup/tasks/alerts → `routes/ops.py`
- Attendance → `routes/attendance.py`
- Health/admin/system → `routes/system.py`

### Add a new database table
Add the `CREATE TABLE IF NOT EXISTS` statement inside `init_db()` in `backend/db.py`.

### Add a new employee
Edit `config/employees.json`. Fields: `id` (empXXX), `name`, `role`, `pin`, `department`, `status`, `whatsapp`.

### Add a new frontend page
1. Create `frontend/yourpage.html`
2. Include `<script src="auth.js"></script>` near the top of `<body>`
3. Include `<script src="shared-config.js"></script>` if you need employee data
4. Use the same `API` base URL pattern

---

## API Route Map

A quick reference map for developers of the massive backend API.

### `app.py` (Core AI & UI Routes)
- **Static:** `/`, `/<path:filename>`, `/uploads/<path:filename>`
- **Chat:** `/api/chat`, `/api/conversations`, `/api/conversations/<id>`, `/api/conversations/<id>/chat`, `/api/conversations/<id>/stream`, `/api/conversations/<id>/title` (PATCH)
- **Huddle:** `/api/conversations/<id>/invite`, `/api/conversations/<id>/huddle-events`
- **Projects:** `/api/projects`, `/api/projects/<id>` (incl. PATCH/DELETE), `/api/projects/<id>/instructions`, `/api/projects/<id>/files` (incl. DELETE `/files/<file_id>`), `/api/projects/<id>/knowledge` (incl. DELETE `/knowledge/<doc_id>`), `/api/projects/<id>/conversations`
- **Clients:** `/api/clients`, `/api/clients/<id>`, `/api/client-users/<username>`, `/api/clients/<id>/tasks`, `/api/clients/<id>/auto-tasks`
- **Tasks (Client):** `/api/tasks`, `/api/tasks/<id>` (PATCH), `/api/tasks/<id>/open`, `/api/tasks/<id>/submit`, `/api/tasks/<id>/approve`, `/api/tasks/<id>/reject`, `/api/tasks/<id>/done`
- **Skills:** `/api/skills`, `/api/skills/custom`, `/api/skills/custom/<id>`, `/api/optimize-prompt`
- **Memory:** `/api/memory/<user_id>`, `/api/memory/<user_id>/<memory_id>`
- **Export/Misc:** `/api/html/generate` (+ `/generate/stream`), `/api/html/preview`, `/api/presentation`, `/api/fetch-url`, `/api/upload`
- **Dashboard:** `/api/dashboard/founder`, `/api/blockers` (admin-only; walks `dependencies` for overdue-blocking tasks)
- **Social Media:** `/api/social-media/auto-fill` (bulk-fills empty caption/script fields via Sonnet)
- **DB Admin:** `/api/admin/backup-db`, `/api/admin/restore-db` (POST) — DB backup/restore over HTTP, verify auth before relying on these
- **WhatsApp:** `/whatsapp/webhook`

### `routes/sheets_sync.py` (Google Sheets two-way sync — see gotcha #87)
- **Link management:** `GET/POST/DELETE /api/clients/<id>/google-sheet-link`
- **Push (Lumina→Sheet):** `POST /api/sheets/push/<task_id>` — called fire-and-forget from `applySheetFields()`, no-op for unlinked clients
- **Pull webhook (Sheet→Lumina):** `POST /api/sheets/webhook/<link_token>` — called by the Apps Script `onChange` trigger installed in a linked Sheet

> [!WARNING]
> **Duplicate/dead route definitions in `app.py`:** `GET`/`POST /api/projects` and `GET /api/projects/<id>` are each defined **twice or three times** (~line 1441/1447/1457 vs ~2378/2385/2396 vs ~2486). Flask/Werkzeug matches whichever rule was **registered first**, so only the first block (the `project_store`-based chat Projects/KB feature) is actually live. The second `list_projects`/`create_project`/`get_project` block (~2378-2401) and the "PROJECT TRACKER ROUTES" block starting ~2486 (which reuses names like `get_projects`/`get_clients` for a *different* clients+tasks entity model) are **unreachable dead code** — but that same dead block also defines helper functions (`_pt_conn`, `_calc_progress`, `_is_admin`) that ARE used elsewhere. Before editing anything named `get_projects`/`list_projects`/`get_clients` in `app.py`, check whether it's actually reachable — it's easy to "fix" dead code by mistake.

### `routes/ops.py` (Standups, Operations, AI Actions)
- **Standups:** `/api/standup`, `/api/standup/today`, `/api/standup/history`, `/api/standup/my-tasks`, `/api/standup/auto-fill`, `/api/standup/carry-over`, `/api/standup/smart-add`
- **Notion Sync:** `/api/notion/status`, `/api/notion/clients`, `/api/notion/tasks`, `/api/notion/dashboard`
- **AI Analytics:** `/api/ai/breakdown`, `/api/ai/proof-of-work`, `/api/ai/coach`, `/api/ai/daily-summary` (`/api/ai/meeting-to-tasks` removed 2026-07-20, see gotcha #46)
- **Client Portal:** `/api/client-portal/tasks`, `/api/client-portal/tasks/<id>/feedback`, `/api/client-portal/dependencies/upload`, `/api/client-portal/dependencies/text`, `/api/client-portal/dependencies` (GET/DELETE)
- **Bet (internal fun feature):** `/api/bet` (GET/POST), `/api/bet/question` (POST) — see Key Patterns #12 and security warning above
- **Discovery Forms:** `/api/form-templates` (GET), `/api/form-templates/<id>` (GET/POST), `/api/discovery-submissions` (GET/POST) (`/api/clients/<id>/form-answers` removed 2026-07-20, see Key Patterns #11)
- **Alerts:** `/api/alerts`, `/api/alerts/run-check`
- **Export:** `/api/export`, `/api/export/standup-tasks`

### `routes/attendance.py` (HR)
- **Attendance:** `/api/attendance/checkin`, `/api/attendance/checkout`, `/api/attendance/summary`, `/api/attendance/today`, `/api/attendance/logs`, `/api/attendance/export`
- **Employees:** `/api/employees`, `/api/employees/checkin`, `/api/employees/summary`

### `routes/auth.py` (Security)
- **Employee Login:** `/api/auth/login`, `/api/auth/verify`, `/api/auth/logout`, `/api/auth/change_pin`
- **Client Login:** `/api/auth/client_login`, `/api/auth/client_verify`, `/api/auth/client_logout`
- **Admin Portal:** `/api/auth/admin_portal_login`, `/api/auth/admin_portal_verify`, `/api/auth/admin_portal_logout`

### `routes/system.py` (Health & Budget)
- **Monitoring:** `/api/health`, `/api/routes`
- **Budget/Usage:** `/api/budget`, `/api/usage`, `/api/usage/export`
- **Admin:** `/admin/download-db`, `/admin/upload-db`

---

## Gotchas & Known Issues

1. **Huddle invites** — The invited user's sidebar updates via a 5-second poll (`setInterval(loadConversations, 5000)` in `app.js`). If it seems broken, check that `list_conversations()` in `conversation_store.py` is matching the invited user's ID in `participant_ids`.

2. **Auto-tagging** — Only fires on the **first** message of a conversation (`len(context) == 1`). If `project_id`/`client_id` is already set, it skips. The badge appears after the `done` SSE event triggers a `loadConversations()` refresh.

3. **SQLite on Railway** — The DB lives at `logs/app.db` on a persistent volume. Do NOT store anything important in `/tmp`. Always use the `logs/` directory.

4. **Streaming vs non-streaming** — The primary chat endpoint is `/stream` (SSE). The `/chat` endpoint is non-streaming (legacy). Both must broadcast huddle messages via `_huddle_broadcast()`.

5. **`is_huddle` in stream** — The `conversation_stream()` function extracts `participants` and `is_huddle` from the conversation object. Make sure `conv` is fetched **before** adding the new message, or the participant count will be stale.

6. **Model pinning for thinking** — `should_think()` can override the model to `claude-sonnet-4-6`. Be careful: if a user selects "haiku" via `model_override`, thinking is bypassed (correct behavior).

7. **Standup Smart Add** — When users add tasks to their daily standup, they go through `/api/standup/smart-add`. We disabled the AI from generating a `clean_title` because it was altering users' exact phrasing in frustrating ways. The system now retains the exact `title` the user typed while only using the AI to infer the `client_name`.

8. **Standup Auto-Carry-Over** — When an employee loads their tasks for today (`/api/standup/my-tasks`), if they have no tasks yet, the system automatically carries over pending tasks from their *most recent* active day (using `MAX(date)` before today). This safely handles weekends and skipped days without relying on a hardcoded "yesterday" (`timedelta(days=1)`).

9. **Client Onboarding Flow** — `client-onboard.html` creates the client record only. The success screen offers options to go to the Board or  Add Tasks. To reliably pass context to the Add Tasks screen (and prevent ID type mismatches between Notion UUIDs and SQLite IDs), `client-onboard.html` saves selected services and deadline to `localStorage` (`claude_client_services` and `claude_client_deadlines`) keyed by **both the ID and the lowercase client name**, and passes the name, ID, and deadline in the URL.

10. **Add Tasks UI (2 Steps)** — `add-tasks.html` has been streamlined into exactly 2 steps: "Select Client & Workflows" and "Add Tasks". The layout is permanently wide (`max-width: 1200px`) to accommodate the Social Media Content Calendar spreadsheet. 
    - **Service Auto-Recall**: When opened via onboarding, or when a client is manually selected, the app checks `localStorage` by client name and auto-selects their chosen workflows and target deadline.
    - **Social Media Calendar**: The "Social" workflow renders a specialized full-width HTML table for planning posts with dropdowns for Post Types (Story, Reel, Static, Carousel) and Statuses (Scheduled, In Progress, Need to Start, Posted, Paused, Final).
    - **Standard Workflows**: Tasks for other workflows are rendered as editable lists. All tasks now start completely **Unassigned** so users can skip assigning them until later.
    - The task templates are:
      - **social** (10 tasks): Client Onboarding & Setup → Brand Brief → Content Strategy formulation → Approval on Strategy → Content Ideation → Content Approval (Cal Sheet) → Discussion with Creative → Creative Assigning → Content Creation (Kanban) → Final Drive Link
      - **branding** (8 tasks): Brand Essence → Stylescape → Logo Design Presentation → Logo Iterations → Visual Style → Collateral → Brand Guidelines Content → Brand Guidelines
      - **website** (4 tasks): Concept & Flow → UI 1st Draft Review → Content → Final Build
      - **shoot** (3 tasks): Video Script & Concept → Video Shoot / Production → Video Editing & Post
      - **miscellaneous** (1 task): Custom Deliverable Setup

11. **Employee API Parsing** — The backend `/api/employees` returns an object `{"employees": [...]}`. Frontend scripts must parse this as an array of objects and convert it into a dictionary `{ id: name }` for dropdowns, otherwise `<option>` tags will render as `[object Object]`.

12. **Add Tasks Quick Action** — The ` Add Tasks` button in `dashboard.html`'s Quick Actions is visible to all users. **As of 2026-07-20, `add-tasks.html` and `client-onboard.html` no longer restrict by an admin allowlist** — any logged-in employee can onboard clients / add tasks (see gotcha #60). Correction to an earlier version of this doc: `dashboard.html` itself never had its own `ONBOARD_ADMINS` constant — it just toggles `.admin-only` elements based on the backend-derived `user.is_admin` flag, which is universal now too.

13. **Project Board — Sheets View (Social Media Only)** — `projects.html` includes a per-client **Sheets** tab that is **only visible for Social Media clients**. A client is detected as social if any of its tasks has `service === "Social Media"` or a title matching `/^\[(Story|Static|Reel|Carousel|Post|Video)\]/i`. The `<div class="cb" data-social="...">` attribute stores this flag.
    - The Sheets view renders a spreadsheet-style table with columns: `#`, `Post Day`, `Post Title`, `Type`, `Brief`, `Idea`, `Caption`, `Assigned To`, `Status`, `File (Drive Link)`.
    - **All columns are editable inline**: `Post Day` uses a `<input type="date">`, `Type` and `Status` use `<select>` dropdowns, all other text cells use `contenteditable="true"`.
    - **Status Dropdown (Sheets) — 6 Social Media Options**: `Need to Start`, `Scheduled`, `In Progress`, `Paused`, `Posted`, `Final`. These replaced the old generic 4-option dropdown (`Not Started / In Progress / In Review / Approved`). Old statuses like `approved`/`done`/`not_started` are auto-mapped to their nearest social media equivalent.
    - On edit (onblur / onchange), `saveSheetRow(taskId, cellElem, clientId)` fires and sends a `PATCH` to `/api/notion/tasks/<id>` (Notion mode) or `/api/sqlite/tasks/<id>` (SQLite mode) with `{ new_title, due_date, assigned_to, status, submission_note }`.
    - After saving, `renderClientCalendar(clientId)` is called to immediately reflect changes in the Calendar tab.

14. **Project Board — Edit Button Routing** — Clicking **✏ Edit** in the calendar popup calls `openEditTask(clientId, taskId)`. For **social media clients** this navigates directly to the Sheets tab (opens the client card if collapsed, calls `switchClientTab` to `sheets`, scrolls into view). For **non-social clients** it switches to the Task List tab and highlights the task with an outline.

15. **`/api/sqlite/tasks/<id>` PATCH** — Defined in `backend/routes/ops.py` → `sqlite_patch_task()`. Accepts fields: `new_title` (→ `title`), `assigned_to`, `due_date`, `status`, `progress`, `submission_note` (→ `description`).

16. **Client Onboarding — Portal Credentials** — `client-onboard.html` step 1 includes optional **Username** and **Password** fields under a "Client Portal Access" section. When the form is submitted, these are passed in the POST body as `client_username` and `client_password`. The backend (`routes/ops.py` for Notion mode, `app.py` for SQLite mode) performs a **pre-check** for username uniqueness before creating the client, then inserts into `client_users` table. For Notion clients, the correct field is `client["notion_id"]` (NOT `client["id"]`) — using the wrong key caused a silent `KeyError` that skipped account creation.

17. **Social Media Kanban — Dynamic Columns** — Both `projects.html` (Project Board) and `client-dashboard.html` (Client Portal) now auto-detect social media clients and render **different Kanban column sets**:
    - **Default clients** (Branding / Website / Shoot): 4 columns — To Do → In Progress → In Review → Approved
    - **Social Media clients**: 6 columns —  Need to Start →  Scheduled →  In Progress → ⏸ Paused →  Posted →  Final
    - **Detection logic** (same in both files): a client is social if ANY task has `service` containing "social", OR title containing "social", OR title matching bracket-style post types `/^\[(story|reel|static|carousel|post|ig|instagram|fb|facebook|tiktok|youtube)\]/i`.
    - `STATUS_MAP` / `SOCIAL_STATUS_MAP` in `projects.html` ensures drag-and-drop saves the correct status key. `bindDragEvents(board, isSocial)` now accepts an `isSocial` flag.
    - Legacy aliases `STAGE_COLS = DEFAULT_STAGE_COLS` and `STATUS_MAP = DEFAULT_STATUS_MAP` are kept so no other code breaks.

18. **Client Portal Calendar — Month Pagination** — `client-dashboard.html` calendar now supports navigating to past and future months via **← →** arrow buttons. A global `calDate = new Date()` tracks the current view month. `calPrev()` and `calNext()` decrement/increment the month and re-render. Today's date is only highlighted when viewing the actual current month.

19. **Client Portal Calendar — Hover Status Tooltip** — Hovering over any task pill in the calendar shows a CSS-powered popup tooltip (`.cal-tip`) containing: task title (bold), status with colour coding, assigned-to, and due date. The task pill's left-border colour also reflects the status at a glance (green = Posted, cyan = Final, blue = Scheduled, purple = In Progress, orange = Paused, grey = Need to Start).

20. **Social Media Statuses — Notion Sync** — The six social media status values (`need_to_start`, `scheduled`, `in_progress`, `paused`, `posted`, `final`) must exist as valid options in the **Status** property of the **Customer Onboarding Tasks** Notion database. If the Status property is a Notion "Select" type, Notion will auto-create new options on first use. If it's a "Status" type, options must be added manually in Notion's property settings.

21. **White-label UI & AI Branding Removal** — The UI has been entirely stripped of "AI", "Bot", and "System" terminology, as well as emojis, model chips, optimizer badges, and thinking indicators, in order to look like a standard human-built agency portal. The logo is now a clean, empty orange box (all text like "AP" removed). Future edits should **not** introduce new emojis, AI-related phrasing, or branding text into the logo/UI.

22. **Huddle Real-Time Identity Fix** — When implementing multi-user huddle SSE, always ensure the sender explicitly includes `sender_id` and `sender_name` in the payload (e.g. `bodyPayload` in `sendMessage`). Otherwise, the backend defaults to the conversation creator's identity, resulting in broadcast events incorrectly attributing messages and causing duplication glitches on the sender's screen. The backend saves these identities in the `conversation_store.py` message metadata so `appendMessage` can correctly render historical messages.

23. **WhatsApp Bot (Meta Cloud API)** — An inbound WhatsApp bot distinct from the Twilio outbound notifications in `notifications.py`. Routes: `GET /whatsapp/webhook` (Meta verification handshake) and `POST /whatsapp/webhook` (receives messages). Incoming text is processed by Claude (Haiku, task type `whatsapp`) and the reply is sent via `send_whatsapp_message()` in `app.py`. Always returns HTTP 200 to Meta — Meta retries on any non-200. Requires `WHATSAPP_PHONE_NUMBER_ID`, `WHATSAPP_ACCESS_TOKEN`, and `META_VERIFY_TOKEN` env vars.

24. **Skills — Model Auto-Upgrade** — When a skill specifies `"model": "sonnet"` in `skills.py`, the streaming handler auto-upgrades the request to Sonnet unless the user explicitly sent a `model_override`. This means selecting a skill like `content_writer` or `code_helper` silently bumps the model tier; haiku-tier requests stay haiku only if no skill is active or the skill's model is also `"haiku"`. Custom skills follow the same logic using their `model` column from `custom_skills`.
25. **Social Media Creation Dates & Standups** — For social media tasks, the "Creation Date" is natively extracted from a dedicated Notion Date property or Created Time property (bypassing the old method of parsing the text description). 
    - **Calendar View**: The task appears as a normal pill on its `Post Day` (due_date). It ALSO appears as a blue `[Start]` pill on its `Creation Date`, but ONLY if the Creation Date has arrived (i.e., `<= today`). Future creation dates are hidden from the calendar to avoid clutter.
    - **Standup Auto-Fill**: Social media tasks bypass the normal "pull if due within 7 days" logic. They are ONLY auto-filled into a user's daily standup if today's date EXACTLY matches the Creation Date (meaning the work starts today), or if they are already `in_progress`, or if they reach their actual Post Day (`due_date`). This prevents premature cluttering of standups with future social media posts.

26. **Notion People Properties & Auto-Fill** — The "Assigned To" column in Notion may be a `people` property rather than a simple `multi_select` dropdown. 
    - **Parsing**: `notion_store.py` extracts the full names from `people` properties (e.g., "Abhinav Gupta"). 
    - **Fuzzy Matching**: Since the local employee mappings (`config/employees.json`) use first names ("Abhinav"), the backend uses fuzzy substring matching (`ename.lower() in notion_name.lower()`) to safely map tasks.
    - **API Filtering Bypass**: The Notion API errors out if you attempt to use a `multi_select` filter on a `people` property. Therefore, the "Auto-Fill" button (which only requests tasks for a specific user) fetches *all* tasks from Notion and filters them entirely in Python, avoiding API query rejections.
    
27. **Notion Column Fallbacks (Title Extraction)** — When determining a task's title from Notion, `notion_store.py` checks for the following properties in order: `"Task"` -> `"Post Title"` -> `"Post"`. This ensures tasks from Social Media boards (which use "Post 17") are not skipped as "Untitled" tasks.

28. **Welcome Screen Initialization**: `showWelcomeScreen()` MUST be called during `DOMContentLoaded` if no specific conversation is loaded (i.e. `!convIdParam`). This ensures that DOM elements like `file-chips` are correctly moved from the hidden `chat-view` to `welcome-input-wrap`. Failing to do so causes pasted images on the home screen to append chips to a hidden container and can trigger native scrollbars in the empty textarea.

29. **Chat Interface Widths**: The main chat container elements (`.msg`, `.input-bar`, `#welcome-input-wrap`) use `max-width: 980px` to provide a wider reading area. Avoid reducing this to prevent the UI from feeling cramped.

30. **Usage Tracking UI**: The frontend mini-budget tracker displays **Overall Usage** (`total_spent_ever`), not monthly usage, and does not render a progress bar against a monthly limit.

31. **Multi-User Identity & Branding in Chat**: User messages display a right-aligned name above their chat bubbles to differentiate senders in multi-user huddles. The assistant's avatar explicitly renders the "OPs" brand text (do not use placeholder initials like "AP" in `appendTyping()`).

32. **Persistent Client Dependencies & File Uploads** — Client projects have a "Dependencies" section for Notes, Links, Images, Videos, and Voice Notes. These are permanently stored in the `dependencies` SQLite table. File uploads (including recorded voice notes) are saved locally to the backend `uploads/` directory using standard `werkzeug.utils.secure_filename` logic.
    - **Client Dashboard Modals:** Uploaded assets are hidden behind "Open" or "Open Folder" buttons that trigger spacious, file-explorer-style modals. Inside the modal, files are intelligently grouped by upload date categories ("Today", "Yesterday", "Last week", etc.).
    - **Specialized Views:** Drive Links bypass the grid layout and are displayed as a continuous serialized numbered list. Notes are rendered as iPhone-style full-width rounded yellow cards with proper text wrapping and timestamps.

33. **Native Voice Recorder** — The client portal includes a native browser voice recorder using `navigator.mediaDevices.getUserMedia` and the `MediaRecorder` API. It records audio chunks, compiles them into a single `audio/webm` Blob, and uploads them to the server just like a regular file attachment. It does NOT use a 3rd-party library.

34. **Daily Standup & Notion Integration** — The "Unassigned Tasks" pseudo-client generated by Notion (tasks with no assigned client) has been repurposed as "**Daily Standup Tasks**" and forced into the **Miscellaneous** tab on the Project Board. Furthermore, `/api/notion/dashboard` intercepts these Notion tasks and cross-references them with the local SQLite `standup_tasks` table:
    - If a task is marked `done` in the local Standup DB, it is **hidden/filtered out** from the Project Board entirely.
    - If a task has `subtasks` defined in the Standup DB (stored as a JSON string), they are attached to the task object and beautifully rendered as a checklist directly on the Kanban task card in `projects.html`.

35. **Mobile Optimization** — The client dashboard (`client-dashboard.html`) and associated portal modals have been thoroughly optimized for mobile screens. This includes flexible wrap layouts, stacked modals, responsive calendar grids, and hamburger/scrollable tab menus, ensuring external clients have a flawless experience on their phones.

36. **SQLite Datetime Parsing in JavaScript** — The server returns dates in standard SQL format (`YYYY-MM-DD HH:MM:SS`). When parsing these into native JavaScript `Date` objects on the frontend using `new Date(dateStr + 'Z')`, some browsers (Safari/Chrome) may throw an "Invalid Date" error. Always convert the space to a `T` first to strictly conform to the ISO standard: `new Date(dateStr.replace(' ', 'T') + 'Z')`.

37. **`kanban.js` is misleadingly named** — Despite the filename, it contains the **Projects/knowledge-base grid** for `index.html`'s side panel (`loadProjects`, `renderAllProjectsGrid`, `showCreateProjectModal`, `renderProjectKb`). It is unrelated to the actual Kanban board UI, which lives inline inside `projects.html` and `client-dashboard.html` (see #17 above). Don't go looking for the board's drag-and-drop code here.

38. **~~One-off patch scripts masquerading as app files~~ — deleted 2026-07-20.** `frontend/api-costing-edit.py`, `frontend/dashboard-edit.py`, root-level `temp.js`, and `temp_standup.html` were never part of the running app (confirmed zero references anywhere in `backend/`/`frontend/` before deletion) — removed along with the rest of the repo-root clutter, see the note under Directory Structure.

39. **~~Admin allowlists are duplicated and inconsistent~~ — superseded 2026-07-20.** See security warning #6 and gotcha #60: `_is_admin()` is now `bool(user_id)` everywhere, so this no longer applies. Kept here only so old context isn't confusing if referenced from history.

40. **Discovery form vs. client form-answers — don't conflate** — `discovery_submissions` (public intake, no `client_id`) and `client_form_answers` (per-client, keyed by `client_id`) are two separate tables/features that look superficially similar. Only the former has frontend pages (`discovery-form.html`, `client-forms-db.html`); the latter's endpoints (`/api/clients/<id>/form-answers`) have no UI consumer found — treat it as unfinished, not broken, if asked to "fix the client form answers page" (there isn't one yet).

41. **`auto_fill_standup()` (`routes/ops.py`) is fragile — check variable scope before editing.** Had 3 separate `NameError` bugs where `target_uids`/`insert_allowed`/`names` were set inside `if sync_all: ... else: ...` but then referenced unconditionally afterward (once via a stray `for...else` instead of `if...else`), silently 500ing every "Sync All Tasks" / personal "Auto-Fill" click. Fixed, but the pattern is easy to reintroduce here.

42. **Assigning a task in Sheets/Kanban directly inserts into `standup_tasks`** — `PATCH /api/notion/tasks/<id>` (`notion_update_task()`, `routes/ops.py` ~1303) auto-inserts a `pending` row into the assignee's standup for today the moment `assigned_to` changes, *if* the Notion write succeeds. This is independent of the `/api/standup/auto-fill` pull-based flow. If a task doesn't show up after assigning, check whether the Notion PATCH actually succeeded before assuming Auto-Fill is broken.

43. **Notion "Assigned To" property type varies by workspace — don't hardcode `_multi_select()`.** `notion_store.py::update_task()` writes it via `_assigned_to_prop()`, which reads the live Tasks DB schema (`_get_assigned_to_prop_type()`, cached 5min) and builds a `people`/`select`/`multi_select` payload to match. Notion rejects the **entire** PATCH if any one property's shape is wrong — a bad assumption here used to silently kill status/title/due_date updates bundled in the same request too.

44. **`auto_fill_standup()` — watch for variables assigned inside a per-task `if matched_id:` block then read again outside it.** A stray `matched_id` reference after the loop used a stale value from a prior iteration, silently marking the wrong row `done` (or crashing when nothing had matched yet). Same family of bug as #41 — this file has a repeated tendency to leak loop-local state past its scope. Before editing any conditional block inside the finished-tasks/pending-tasks loops here, trace every variable back to where it was last assigned.

45. **`notion_update_task()` "Untitled Task" bug — Sheets checkbox assign-only edits never send a title.** `saveSheetRow()` (`projects.html`) sends a **diff-only** PATCH body: if the user only toggled an assignee checkbox, `new_title`/`task_title` are omitted entirely. The backend used to hard-fallback to the literal string `"Untitled Task"` in that case. Fixed by adding `notion_store.get_task_summary(notion_id)`, which fetches the live Notion page and builds `"{client_name} — {title} ({content preview})"` when the request didn't supply a title. Any other endpoint that accepts a diff-only payload from `saveSheetRow()` should assume `new_title` may be absent and NOT treat that as "task has no title" — fetch it from Notion instead of hardcoding a placeholder.

46. **Project Board header nav** — `projects.html`'s header no longer has a "Paste Meeting Notes" button; it was replaced with a "Daily Standup" link (`<a href="standup.html">`) per user request. The `openMeetingNotesModal()`/`parseMeetingNotes()` functions, their modal markup, and the backing `/api/ai/meeting-to-tasks` route (`routes/ops.py`) were fully **removed 2026-07-20** — confirmed zero remaining references anywhere before deleting. If meeting-notes-to-tasks is wanted again, it'll need to be rebuilt from scratch (check git history around commit `2b611d7` and earlier for the last working version), not resurrected — there is no dead code left to re-wire.

47. **API Costing dashboard "Remaining" is NOT the real Anthropic Console credit balance.** `budget_tracker.py`'s numbers (`monthly_spend`, `total_spent_ever`, `budget_limit`) are the app's own per-call cost estimate against the static `MONTHLY_BUDGET_LIMIT` env (default $20) — Anthropic exposes no API to read actual org credit balance, so there's no way to keep this in sync automatically. `GET/POST /api/account-balance` (`routes/system.py`) stores a manually-entered real balance in `app_settings` (key `account_balance`); `api-costing.html`'s REMAINING KPI is clickable and prompts to update it. When this manual balance is set, the frontend also recomputes `limit` as `total_spent_ever + accountBalance` (instead of the static $20) so Spent%/warning/critical thresholds/progress bar all stay internally consistent with the real number — don't reintroduce a raw `d.budget_limit` reference elsewhere in that file or it'll drift out of sync again. Remember this override is per-environment: seeding it in a local `logs/app.db` does nothing for the Railway prod DB (and vice versa) — must be set separately in each.

48. **Two Railway projects existed pointing at the same GitHub repo/branch.** `diplomatic-kindness` (custom domain `lumina.mmga.agency`, has the persistent `web-volume` attached — the real DB lives here) was the actual production project. `pleasant-flow` (default `*.up.railway.app` domain, no volume) was a duplicate/stray project with auto-deploy also wired to the same repo+`main` branch, so every push deployed to both. If you see two Railway projects tracking this repo, check for the attached volume and the custom domain to identify which one is real before touching either — the one with `web-volume` and `lumina.mmga.agency` is production.

49. **HTML generated in chat auto-downloads and is prose-free.** The `html_design` task prompt (`backend/app.py` `SYSTEM_PROMPTS["html_design"]`) now instructs the model to reply with ONLY a single ```html code block — no intro/explanation before or after. On the stream `done` event, `frontend/app.js` extracts that fence via `extractFirstHtmlFence()` (from `utils.js`) and auto-triggers a download via `downloadHtmlArtifact()` (filename derived from the conversation title), so the user no longer has to click the code block's manual "Download" button. This only fires when `event.task_type === "html_design"` — verified end-to-end locally via chrome-devtools MCP (chat request → sidebar tagged `html_design` → response was code-only → toast "Downloading HTML file…" → file landed in Downloads with correct content).

50. **Social task "Creation Date" is now a real Notion date property, not text parsed from Notes.** Previously the Sheets UI wrote `Creation Date: YYYY-MM-DD` into the task's `Notes` rich-text property and the standup logic regex-parsed it back out — editing the date in Sheets never actually changed anything machine-readable, so postponing a task's creation date silently had zero effect on whether it showed in the daily standup.
    - Fixed with three coordinated changes (`backend/notion_store.py`, `backend/routes/ops.py`, `frontend/projects.html`), all in one commit:
      1. **Real property + auto-create:** `update_task()` now accepts a `creation_date` kwarg and writes to an actual Notion `Creation Date` date property. Since Notion **rejects an entire page PATCH if it references a property that doesn't exist on the database**, `_ensure_creation_date_property()` lazily PATCHes the *database* schema to add it on first use (cached in-process via `_creation_date_prop_ready`, so it only fires once per server lifetime). `get_task_summary()` now also returns `creation_date` (and `description`) read from the real property, with the old regex-on-Notes match kept only as a fallback for legacy tasks that predate this property existing.
      2. **Standup self-heals stale snapshots:** `GET /api/standup/my-tasks` (`routes/ops.py`) previously never re-checked a task's live Notion state once it was pulled into the local `standup_tasks` snapshot — postponing the Creation Date in Notion afterward did nothing to the already-inserted row. Now, on every load for *today's* date, it calls the new `_task_creation_is_future(notion_id, today_str)` helper for each non-terminal row and deletes any whose live Creation Date has moved into the future.
      3. **`saveSheetRow()` in `projects.html`** now sends `creation_date` as its own diff field (only when changed) instead of bundling it into the free-text Notes blob, and reads `t.creation_date` from the real property first before falling back to regex-parsing Notes.
    - **Bonus bug caught while testing this**: `notion_update_task()` (`PATCH /api/notion/tasks/<id>`) was unconditionally overwriting the `Assigned To` property to blank on *any* request that omitted `assigned_to` from the body, because the handler defaulted it to `""` instead of `None` before passing it down — and `update_task()`'s `is not None` check always passed on an empty string. A diff-only PATCH (e.g. creation-date-only) would silently wipe the assignee. Fixed by making the field properly `None`-passthrough-aware end to end.
    - **Verification note:** since Notion has no per-environment separation (unlike SQLite, which is a separate file per environment), this fix was verified by testing directly against the live production Notion workspace and the deployed `lumina.mmga.agency` endpoint, then restoring any test-mutated task fields back to their original values immediately after.

51. **`notion_store.py::list_tasks()` — Content/Idea/Scripts/Caption/Link parsing from `Notes` used to require a literal `"|"` in the text.** Sheets (`saveSheetRow()` in `projects.html`) builds the Notes blob by joining only the *filled* fields with `" | "` — so a task where the employee only filled in Scripts/Copy (no Content/Idea/Caption) produces a single-field Notes string with zero pipes, e.g. `"Scripts: STORY 1\n\n..."`. The old guard `if not brief and desc and "|" in desc:` skipped the whole parse block whenever there was no pipe, so `scripts_copy` (and any other single-filled field) silently stayed empty — the client portal's Task Feedback modal showed no script even though the employee-facing Sheets view (which parses `Notes` independently, client-side) displayed it fine. Fixed by dropping the `"|" in desc"` requirement (`desc.split("|")` already degrades gracefully to a one-element list when there's no pipe) — condition is now just `if not brief and desc:`. If a "single field only" save ever appears empty on the client side again, check this guard first before assuming it's a Creation-Date-style unwritten-property issue (see #50).

52. **Notion integration audit — 5 more latent bugs fixed in `notion_store.py` in one pass.** Found by reviewing the whole file end-to-end after #50/#51 kept surfacing the same "silent data loss" pattern; all verified against live production Notion data post-fix (task/client counts unchanged).
    - `get_task_summary()` still had the pre-#51 `"|" in desc` requirement — same bug, different function. It's used by the "Untitled Task" title fallback (#45) and the standup Creation-Date self-heal check (#50), so a single-field `Content:`-only task would still summarize wrong even after #51 fixed `list_tasks()`. Now uses the same unconditional `if not content and desc:` guard.
    - `create_task()` hardcoded `_multi_select()` for the `Assigned To` property instead of the schema-aware `_assigned_to_prop()` that `update_task()` already used (see #43). Harmless today because this workspace's `Assigned To` is currently `multi_select`, but `create_task()` is called from 8+ places (client onboarding auto-tasks, standup smart-add, meeting-to-tasks AI, social auto-fill…) — if the property type ever flips to `people` again, every task-creation path silently fails (Notion rejects the whole page-create, swallowed by a broad `except`) with zero visible error. Now matches `update_task()`.
    - `create_client()` used the static `CLIENTS_DB_ID` module constant instead of `_clients_db()` — the one function that didn't hot-reload `NOTION_CLIENTS_DB_ID` changes without a redeploy, unlike every other function in the file.
    - `list_tasks()` / `list_clients()` wrapped the *entire* pagination loop in one `try/except` — one malformed page (bad relation, unexpected null) would blank the whole result list for everyone, not just skip that row. Now each page is parsed inside its own `try/except` that logs and skips just that page.
    - `_fetch_page_title()`'s cache (`_page_title_cache`, used for relation-property title lookups) never expired — a renamed related page would show its old title for the life of the server process. Now uses the same `_CACHE_TTL` (300s) pattern as the other in-process caches in this file.

53. **Several Notion-writing frontend flows treated a failed PATCH as success — fixed in `my-tasks.html` and `projects.html`.** `fetch()` only rejects on network failure, not on HTTP error status, so any call site that `await`s a `fetch(...)` (or `Promise.all` over several) without checking `.ok`/`d.success` will report success even when the backend returned a 500. Found and fixed three: `my-tasks.html::bulkDefer()`/`bulkStart()` used `Promise.all` over raw `fetch()` calls with no per-response check, so a partial or total failure across the batch still showed "All overdue tasks deferred!" / "Sprint started!"; `recalcSubtasks()` only `console.error`'d on failure and still optimistically updated the local progress cache, making a subtask checkbox look saved when Notion never got the write. `saveSheetRow()` in `projects.html` also just threw a generic "Network error" / showed "Error saving row" for any failure — now surfaces the actual `error` field from the backend's JSON response instead. **When adding any new Notion-writing UI action, always check the response status/`success` field before showing a success toast or updating local state optimistically** — this codebase has a recurring pattern of silently-successful-looking failures for exactly this reason.

54. **`client-dashboard.html` and `standup.html` had no `toast()` function — every message used a blocking native `alert()`, fixed.** Every other page in the app (`projects.html`, `app.js`, etc.) already has a `toast(msg, type)` helper (a small styled div, auto-dismisses), but these two never got one, so all their informational/error messages fell back to `alert()` — worst on `client-dashboard.html` since that's the actual **external client portal**: clients were seeing raw unstyled browser popups for things like "Failed to submit feedback." Both files now have the same `toast()` pattern (styled `#toast` div using each file's own existing `--s1`/`--bdr`/`--green`/`--red`/`--rs` CSS vars), and every purely-informational `alert()` was swapped to it. **Left as native `confirm()` on purpose** (don't "fix" these): destructive-action confirmations (delete dependency, delete task) and the one genuine yes/no branch in `standup.html`'s `autoFillStandup()` ("pull upcoming tasks?") — a blocking native confirm is the correct tool for those, only the *informational* `alert()`s were the problem. If you add a new page, give it a `toast()` from the start rather than reaching for `alert()`.
    - **Follow-up sweep, 2026-07-20: every remaining informational `alert()` in `frontend/` was swapped to `toast()`.** Covered `app.js`, `index.html` (both use `showToast()`, defined in `app.js`), `skills.js` (guarded via `typeof showToast === "function"`, matching its own pre-existing pattern), `project.html` (uses `project.js`'s existing `showToast()`), `add-tasks.html`/`projects.html` (already had `toast()`, just needed the remaining call sites swapped), and five files that had **no toast infra at all** — `api-costing.html`, `bet.html`, `client-admin.html`, `html-generator.html`, `presentation.html` — each got the same minimal `#toast` div + `toast(msg, type="ok"|"err")` function, styled with whatever CSS vars that file already defines (falls back to a literal `#ef4444` red where a file has no `--red`/`--grn` var, e.g. `html-generator.html`/`presentation.html`). `prompt()` in `api-costing.html`'s `editAccountBalance()` was deliberately left alone — it's a blocking input, not an informational message, no toast equivalent applies. As of this sweep, `grep -rn "alert(" frontend/` returns zero real hits (only a string literal containing the word "alert" in a toast message on `standup.html`).

55. **Sheets view — fast trackpad swipe could trigger browser back-navigation despite `overscroll-behavior-x: none` already being set.** (`projects.html`) A known Chromium high-velocity-swipe edge case on Windows: CSS `overscroll-behavior` alone doesn't always catch it. Fixed with a JS `wheel` listener (`preventEdgeSwipeNav()`) that `preventDefault()`s only when the horizontal delta is dominant AND the container is already at its scroll edge — applied to both the regular and fullscreen Sheets containers (same DOM element, re-rendered). Also added `overscroll-behavior-x: none` on `html,body` as a second layer. If a similar "swipe navigates back" report comes in for another horizontally-scrollable table, reuse `preventEdgeSwipeNav()` rather than assuming the CSS property alone will fix it.

56. **Full-app audit (non-security) found and fixed 16 bugs in one pass — see commit `1908b20`.** Two were completely broken features with zero errors surfaced anywhere: `client-admin.html` had a `letDashboardData`/`DashboardData`/`dashboardData` typo (missing space merged `let` into the variable name) that threw `ReferenceError` on every load, leaving the admin sidebar stuck on "Loading clients..." forever; `_parse_slides()` in `app.py` never appended slides or returned anything, so `POST /api/presentation` 500'd on literally every call (presentation builder was fully non-functional). Also: `PATCH`/`DELETE /api/projects/<id>` called `project_store.update_project()`/`delete_project()`, which didn't exist (added them) — and while fixing that, found `_build_system_prompt()` was checking `project["custom_instructions"]`, a key `get_project()` never returns (real key is `instructions`), which silently dropped project-level instructions/memory from every non-streaming chat, `html_design`, presentation, and the WhatsApp bot's system prompt (the streaming endpoint reads the correct key and was never affected). Full list of all 16 fixes is in the commit message — worth reading before assuming any of these areas (`client-admin.html`, presentation generation, project CRUD, standup/budget date logic, `app.js` conversation-switching/rename/retry, `add-tasks.html`) are still broken; they were fixed and verified (pyflakes clean, Flask boots, `node --check` passes, and the riskiest fixes were exercised directly with real inputs) in this pass. One deliberate style choice made during this pass: `ops.py`'s ~21 `datetime.utcnow().strftime("%Y-%m-%d")` date-bucketing call sites were switched to `today_ist()` (already-imported but previously-unused) since this app is IST-based throughout — but true absolute timestamps (e.g. `.isoformat()+"Z"` audit-log fields) were deliberately left on UTC. Don't blanket-convert every `datetime.utcnow()` in this codebase to IST — only the "what date is today" bucketing kind.

57. **Same audit, round 2 — 6 more bugs fixed, see commit `8bf38bf`.**
    - `auto_generate_tasks()` (`app.py`) only saved founder-typed `extra_notes` for SQLite-mode clients (appended to `clients.requirements`); Notion-mode clients (the common case) silently discarded them. Added `notion_store.append_client_requirements()` (fetch-then-append, since Notion has no native string-append operation) and wired it into the Notion branch.
    - `ops.py::ai_priority_advisor()` (`/api/standup/ai-coach`) had no try/except around its Claude call, unlike every sibling AI route — a timeout/rate-limit returned a raw Flask 500 HTML page instead of JSON.
    - `ops.py::ai_proof_of_work()` used `st["text"]` instead of `st.get("text", "")` — a subtask dict missing that key threw `KeyError` and 500'd the whole "generate proof of work" request.
    - `app.js::startNewChat()` used to always resolve (it catches its own errors and never re-throws), so `kanban.js`'s project-chat "Send" button's `.then()` always ran as if it succeeded, clearing the input into a conversation that might not exist. `startNewChat()` now returns `true`/`false`; `kanban.js` checks it and restores the typed message + project view on failure instead of losing it silently.
    - `appendErrorMessage()` bubbles (class `"msg error"`) were counted by the same `.msg` selector used to compute each message's server-side index in three places (`appendMessage`, `regenerateWithNote`, `sendMessage`'s truncate-from-index logic) — a failed send inflated the DOM's message count relative to the backend's saved list, so a later Edit/Retry could target the wrong message. All three now use `.msg:not(.error)`.
    - `add-tasks.html`'s `loadClients()`/`loadEmployees()` never checked `res.ok`, and the `DOMContentLoaded` init had no try/catch around them — either endpoint erroring left the client dropdown stuck on "Loading clients..." forever with zero feedback. Both now check `res.ok` and init shows a visible error state on failure.

58. **Two recurrence-prevention tools added, see commit `e16fbba`.**
    - `scripts/pre-commit` — a git pre-commit hook that blocks commits introducing pyflakes "undefined name" errors in staged `.py` files (the variable-scope NameError pattern this repo keeps reintroducing — see gotchas #41/#44/etc). Originally **not installed automatically** (`.git/hooks/` isn't tracked by git); **as of 2026-07-20, `start.sh` auto-installs it** on every run if `.git/hooks/pre-commit` is missing, so a fresh checkout only needs to run `bash start.sh` once to get it — no more manual `cp`/`chmod` step needed. Only blocks on "undefined name", not on the large pre-existing body of unused-import/unused-variable warnings — don't tighten this without checking how much pre-existing noise it'd start blocking on.
    - `GET /api/notion/schema-check` (`notion_store.get_schema_report()`) — compares the live Notion Tasks/Clients DB schema against what the code actually reads/writes, split into `missing_required` (core functionality breaks) vs `present_fallbacks` (documents which of several fallback property names are actually present — informational, not an error). Hit this whenever something reading Notion data starts silently returning empty values, or after editing Notion properties by hand — it was built specifically because the Creation Date / Scripts / extra_notes bugs (gotchas #50/#51/#57) were all the same root cause: a property renamed, missing, or never created, silently degrading instead of erroring.

59. **Continued full-app audit (round 3) — 14 more bugs found and fixed, see commit `2b611d7`.** Two standouts:
    - `send_whatsapp_message()` (`app.py`) read `META_WHATSAPP_TOKEN` — a name that appears nowhere else in the repo or its docs, the real env var is `WHATSAPP_ACCESS_TOKEN` (see the Environment Variables table above). This meant the WhatsApp bot (gotcha #23) has never been able to send a single reply since it was built: `token` was always `None`, the early-return guard fired silently on every inbound message, and the webhook still correctly returned 200 to Meta (per its own requirement) so nothing ever surfaced the failure anywhere. Fixed the env var name and added a response-status check (previously the Meta API call's result was discarded entirely, so even a 401/429 from Meta looked identical to a successful send).
    - The **entire welcome-screen "+" menu was dead** (`skills.js`) — `index.html`'s welcome screen has a fully parallel, separately-ID'd copy of the plus-menu (`welcome-menu-web-search`, `welcome-menu-manage-skills`, `welcome-plus-btn`, etc.) that `skills.js` never wired up at all; only the file-upload item worked (it had its own dedicated listener). Same story for `optimizer.js`'s Optimize button (`welcome-optimize-btn`/`welcome-input` were never referenced anywhere). Both were refactored to wire the main and welcome screens through one shared function instead of hand-duplicating listeners per screen, specifically so this class of "added a feature to the main menu, forgot the welcome one exists" bug can't recur — **when adding any new plus-menu item, wire it through `wirePlusMenu()`'s config object, don't hand-attach a new listener to just `#plus-btn`'s menu.** Also found the welcome menu's "Use style" submenu had completely different, half-built markup (`Manage styles`/`Add style`/dynamic list) with no backing feature anywhere in the codebase (styles are a fixed enum: normal/concise/detailed/formal, no custom-style CRUD exists) — replaced with the same working 4-option markup the main menu uses.
    - Also fixed: non-idempotent `usage_logs` legacy-JSON migration in `db.py` (could double-count spend on a crash-mid-migration), several `background:var(--x),color)` invalid-CSS-gradient typos (missing `linear-gradient(...)` wrapper) in `client-portal.html`/`html-generator.html`/`presentation.html`, `html-generator.html`'s Generate button getting stuck disabled forever if the SSE stream dropped before a terminal event, and three double-submit-guard gaps (`bet.html` vote, `login.html`'s on-screen numpad backspace, `client-login.html`'s login button).

60. **Admin access opened to all employees; Noorish (emp009, HR) added — 2026-07-20, commit `502df14`.** User request: give every employee full admin-tier access (view all attendance, onboard clients, etc.), keep the unrelated **bet** feature restricted to its existing 4-person allowlist.
    - `config/employees.json` — added `emp009` (Noorish, HR, PIN `0000`).
    - `backend/utils.py::_is_admin()` and `backend/app.py::_is_admin()` (the two duplicate admin-check functions from security warning #6 / old gotcha #39) both changed from a hardcoded employee-ID list to `return bool(user_id)` — any logged-in employee is now admin. This is a deliberate product decision, not a bug fix; it also incidentally resolves the "4 places to update" inconsistency problem, since there's no longer a list to keep in sync.
    - `frontend/add-tasks.html` and `frontend/client-onboard.html` — removed their local `ONBOARD_ADMINS` arrays; gate is now just "is a user logged in" (`if (!user.user_id)`).
    - `frontend/dashboard.html`'s `.admin-only` UI elements (Onboard Client, Backup DB, Copy Client Link) and Export Attendance button needed **no edit** — they already read the `is_admin` flag from the login/verify response, which now flows from the changed backend `_is_admin()` automatically.
    - `frontend/bet.html` and `routes/ops.py`'s bet endpoints — confirmed unchanged, still `["emp002","emp003","emp007","emp008"]` (Nupur/Abhinav/Palak/Happy) exactly as requested.
    - **Known gap, flagged not fixed:** `routes/attendance.py::attendance_logs()`/`attendance_export()` take `user_id` from an unauthenticated query param (`request.args.get("user_id")`), not a session cookie. Before this change that leaked attendance data only to guessers of a real admin ID; now `bool(user_id)` means *any* non-empty string in that query param grants access — a bigger exposure than "let our own employees see each other's attendance." Worth session-verifying those two routes properly if this app is ever exposed beyond trusted internal use.

61. **Repo-cleanliness pass — 2026-07-20.** User asked for a general "what would make this better" pass; scope was six independent fixes, all verified (pyflakes clean on `ops.py`, `node --check` clean on every edited HTML `<script>` block, `import app` boots without error).
    - Deleted `client_form_answers` dead endpoints (see Key Patterns #11) and the entire meeting-notes-to-tasks feature (see gotcha #46) — both zero-reference dead code confirmed by grep before deletion.
    - Deleted all repo-root/`backend` one-off debug scripts and stray `.db` files (see Directory Structure note and gotcha #38); added `.gitignore` rules so they can't silently reaccumulate.
    - `start.sh` now auto-installs `scripts/pre-commit` on first run if missing (see gotcha #58) instead of requiring a manual `cp`/`chmod` step.
    - Swept every remaining informational `alert()` in `frontend/` to `toast()` (see gotcha #54's follow-up note) — five files gained toast infra for the first time (`api-costing.html`, `bet.html`, `client-admin.html`, `html-generator.html`, `presentation.html`).
    - Checked `dashboard.html` for a claimed dead `ONBOARD_ADMINS` constant from an earlier (incorrect) note in this doc — it never existed there; corrected gotcha #12 accordingly. Lesson: don't trust a prior session's claim about a specific file's contents without grepping it first, even in this same document.

62. **Sheets row delete/undo (gotcha #61-adjacent) generalized into a spreadsheet-style Sheets view — 2026-07-20, commit `1da2377`.** `projects.html`'s Sheets tab (`renderClientSheets()`) gained: click-to-sort headers (asc/desc/reset cycle, stored in `sheetSortState`), full-text row search (`sheetSearchState`), a Columns show/hide menu (`sheetsHiddenCols_<clientId>` in localStorage), a frozen first column while scrolling horizontally (header was already sticky-top via existing `.c-sheets th` CSS), row checkboxes with bulk status-change/bulk-delete, a right-click context menu (insert row above/below, duplicate row, clear cell, delete row), tab-separated paste from Excel/Google Sheets across multiple cells/rows with matching block-copy back out, click-drag/shift-click cell range selection with Delete/Backspace bulk-clear, and boundary-aware Left/Right arrow-key cell hopping (only at the very start/end of a text cell's content, so normal in-cell caret movement is untouched) plus Up/Down arrow nav. **Enter/Shift+Enter row-to-row navigation was added then removed same day** (commit `70b9e92`) per user feedback — Enter now behaves natively in contenteditable cells again; don't re-add it without checking this note first. A "Saving…/Saved ✓/Save failed ✕" indicator replaced the previous silent save-on-blur. The single-row undo-bar mechanism from gotcha #61 (`pendingRowDelete`) was generalized to hold an array of items so bulk delete shares the same 8-second undo window instead of a separate code path. `renderClientSheets()` was refactored to first build a `records` array (deriving type/title/content/idea/scripts/caption/link/notes/status/assignee from each raw task) so sort, search, and rendering all read from the same shape instead of three different ad-hoc derivations.
    - **Real bug found and fixed during testing, not by inspection:** a pre-existing, unrelated `document.addEventListener('paste', ...)` listener further down the same file (added in an earlier session — forces plain-text paste in *any* `[contenteditable="true"]` element via `execCommand('insertText', ...)`) fires on the exact same paste event as the new Sheets multi-cell paste handler. Since both are `document`-level listeners for the same event, both ran and the second one clobbered/duplicated whatever the first had just written. Fixed with `e.stopImmediatePropagation()` in the new handler once it decides to actually handle a paste — the old listener still runs standalone for contenteditable elements outside `.c-sheets` (e.g. the header-rename spans), where the new handler doesn't apply. **If a future paste-related bug appears anywhere in `projects.html`, check for listener collisions like this first** — `document.addEventListener('paste', ...)` now appears twice in the file by design, not by accident.
    - **Testing methodology:** tested live against a local Flask server with a real (already-existing) employee session — but `window.fetch` was intercepted for all `/api/(notion/tasks|sqlite/tasks|tasks)` write calls (POST/PATCH/DELETE) to return canned success JSON, and `allData` was replaced with synthetic in-memory client/task records rendered into a throwaway `#csheets-test1` container appended to `<body>`. Every feature was exercised via real keyboard/mouse/clipboard events (Ctrl+Z, Enter/Arrow nav, drag-select + Ctrl+C, synthetic `paste`/`copy` events, real DOM clicks) with zero console errors — but no test write ever reached the real database or the production Notion workspace. Read-only GETs (the page's own initial dashboard load) did hit real production Notion, which is how the paste-listener collision bug surfaced: mid-testing, that real initial load's async response resolved late and silently overwrote the injected mock `allData`, briefly making `undoRowDelete()` a no-op (correctly, since the client had "disappeared" from `allData` — not a bug, just a test-harness race). Re-injecting the mock after the real load settled fixed the harness; worth remembering if similar live-page testing is done again on `projects.html`.

63. **`EMP_NAMES` hardcoded dicts (the ones flagged as "harmless except-block fallbacks" in gotcha #52) turned out to have one real, active, non-fallback usage — found 2026-07-29 while debugging "assigned a task to Anagha, doesn't show in her standup."** `notion_update_task()` (`routes/ops.py`, the handler behind `PATCH /api/notion/tasks/<id>` — i.e. every assignment made via Sheets or Kanban) used a **hardcoded 7-name `EMP_NAMES` dict unconditionally**, not as an exception fallback, to translate the raw employee-id checkbox value (e.g. `"emp010"`) into a display name before writing it to Notion's `Assigned To` property. New hires (Noorish/emp009, Anagha/emp010) aren't in that dict, so `EMP_NAMES.get(a, a)` fell through to the raw id itself — writing the literal string `"emp010"` into Notion instead of `"Anagha"`.
    - **Why this broke the standup, not just the display:** the Sheets UI's own checkbox still looked correct (`isSelected` fallback-matches by raw id, `a === String(e.id)`), so nothing looked wrong in our app. But `auto_fill_standup()` (`/api/standup/auto-fill`, "Sync All Tasks") matches assignees by **name substring only** (`ename.lower() in n.lower()`), with no id fallback — `"anagha" in "emp010"` is false. So once the bad value landed in Notion, *both* the automatic push-on-assign path (which uses the raw id correctly and would've worked) and the only manual recovery path (Sync All Tasks, which needs a real name) were dead ends for that specific task — assigning her again via Sheets wouldn't have self-healed it either, since it'd hit the same broken `EMP_NAMES` dict every time.
    - **Confirmed live via the Notion API (read-only) before touching anything**, per systematic-debugging: fetched the task directly, saw `assigned_to: "emp010"` and `creation_date`/`due_date` both `2026-07-29` (today) — ruling out the creation-date self-heal (gotcha #50) as the cause, since that only deletes rows whose creation date is *strictly after* today. Also found the exact same pattern already live in **3 more places**: `delegate_task()` (~line 448), a second copy inside `notion_update_task()`'s WhatsApp-notification branch, and `auto_generate_tasks()`'s custom-task branch (~line 1258, which additionally had `"emp007": "Tanaya"` — stale, since emp007 is Palak now) — plus one more in `app.py`'s `get_blockers()`. None of the 5 are legitimate fallbacks; all read live `employees.json` correctly elsewhere in the same file (e.g. gotcha's own reference pattern at `ops.py` ~line 191) but not here.
    - **Fix:** added `_load_emp_names()` (`routes/ops.py`, near the other helpers) — reads `utils._load_employees()` live, falls back to the old hardcoded 7-name dict only if the file read itself throws. Replaced all 5 hardcoded dicts (4 in `ops.py`, 1 in `app.py`) with calls to it. Verified: pyflakes clean (actually *removed* two pre-existing "imported but unused" warnings for `utils._load_employees`, since it's now finally used), `import app` boots clean.
    - **Data repair, done live against production Notion (read-only investigation first, writes only after confirming zero collateral impact):** the already-corrupted "Confidence" task (Mellow client, `notion_id=391cacb2-...`) was re-saved through the now-fixed `PATCH /api/notion/tasks/<id>` endpoint, which correctly wrote `"Anagha"` this time. Before deleting any of the leftover garbage tags visible directly in Notion's own property editor (`emp006`, `emp002`, `emp007`, `emp010`, a stray `-`, and a garbled `"— Assign —\nVidit\nNupur\n..."` tag — 6 total), **every task in the workspace was checked for whether it still referenced one of those exact option IDs** (deleting a Notion select/multi_select option that a task still holds silently blanks that task's value — not just a display cleanup). Confirmed exactly one live reference (the Confidence task, fixed first) and zero others, then removed the 6 junk options from the `Assigned To` property schema via a direct `PATCH /v1/databases/<id>` call (had to resend the *entire* options array with real ids/colors preserved for the ones to keep — Notion has no "delete just this option" endpoint). Verified after: diffed every task's `assigned_to` value before vs. after the cleanup — zero unintended changes. Real name tags that aren't in `employees.json` (`Raj`, `Tanaya`, `Kashmira`) were deliberately left alone — they're presumably freelancers/former staff tracked only in Notion, not garbage.
    - **If this pattern recurs:** any code that needs an id→name mapping in this file should call `_load_emp_names()`. Don't add a new inline `EMP_NAMES = {...}` dict, hardcoded or not — that's exactly how this bug happened five times in one file.

64. **Ticking a task done in Daily Standup already pushes its Notion status to "Need for approval" for social-media (Sheets) tasks — but the detection of "is this a social-media task" was reading an unreliable Notion property, fixed 2026-07-29.** `update_my_task()` (`PATCH /api/standup/my-tasks/<id>`, `routes/ops.py` ~994) already had the logic: when a task with a `notion_id` is marked done (frontend sends `progress: 100` alongside `status: 'done'`, see `toggleTask()` in `standup.html`), it calls `notion_store.get_task_type(notion_id)` and, if that says "social media", overrides the Notion status to `"need_for_approval"` (→ "Need for approval" via the status map) instead of `"Done"`. This is exactly the behavior the user asked for, and it looks like a from-scratch feature request but isn't — it was already built.
    - **The actual bug:** `get_task_type()` only read the Notion `Type`/`Task Type` **properties**, with no fallback. But per Key Patterns #13/#17, the app's own "is this social media" detection everywhere else (`renderClientSheets()`'s filter, the Kanban column-set detection) never relies solely on that property — it always ALSO falls back to matching a `[Story]`/`[Reel]`/`[Static]`/`[Carousel]`/`[Post]`/`[Video]` bracket prefix in the title, because Sheets-created rows don't reliably get `Task Type` populated. `get_task_type()` was the one place that fallback was missing. Confirmed live: the "Confidence" task (Mellow, `notion_id=391cacb2-...`) had `service: ""` (empty `Task Type`) despite being a genuine `[Story]` row — so ticking it done in standup would have silently set Notion status to `"Done"` instead of `"Need for approval"`, with no error anywhere.
    - **Fix:** `get_task_type()` (`notion_store.py`) now falls back to the same bracket-title regex when both properties are empty, returning `"Social Media"` in that case — mirrors the detection logic already used elsewhere instead of inventing a new one.
    - **Verified live, fully reversible:** created a throwaway local `standup_tasks` row (`POST /api/standup/my-tasks`) pointing at the real Confidence task's `notion_id`, PATCHed it to `done`+`progress:100` exactly like the frontend does on tick, confirmed the real Notion task flipped to `status: "Need for approval", progress: 100`, then restored it to its original `Not Started`/`0` and deleted the local test row.

65. **Clicking a standup task without a `notion_id` could jump to a completely unrelated row in Sheets — found and fixed via code tracing only, 2026-07-29, no live Notion lookup needed.** `GET /api/standup/today` (`routes/ops.py` ~187, powers the Team Standups panel that `standup.html` renders — this is the view a user's own tasks show up in, since the sort places `UID` first) never selects the real `standup_tasks.id` column; it fabricates one with `"id": len(tasks_by_user[uid])` — just a per-user 0,1,2… position counter, not a real id. `standup.html`'s task-title links (`New Tasks` ~line 736, `Carried Over` ~line 778) built their `projects.html?notion_id=...` URL as `task.notion_id || task.id` — so any task with no real `notion_id` (a personal/non-project standup item never pushed to the board) sent that tiny fake counter instead. On `projects.html`, no task has an `id` that small (Notion-mode tasks use `t.id = t.notion_id`, a UUID), so the exact-id match always failed, and the code fell through to a very loose fuzzy title match (`if (nt.length > 2 && tt.includes(nt)) return true` — any existing task whose title is a short substring of the clicked task's title counts as a "match"). That grabs the first coincidentally-similar-titled task anywhere on the board and highlights it as if it were the real one — explaining a report of "clicked a standup task, landed on some unrelated Sheets row that's unassigned with no dates."
    - **Fix:** `standup.html` now only renders the title as a clickable `<a href="projects.html?notion_id=...">` when `task.notion_id` is truthy (mirrors the existing convention in the personal task list, which already shows a "↗ push to board" button instead of a link when `notion_id` is missing — see `renderTasks()` ~line 384). Tasks with no Notion link are now a plain, non-clickable title — there's genuinely nothing on the board to navigate to. The `|| task.id` fallback was removed entirely rather than fixed, since the backend's fake counter has no other legitimate use as a lookup key.
    - **Related, separately confirmed bug in the same investigation:** `notion_store.create_task()` never wrote a `Creation Date` property at all, for any caller — unlike `update_task()`, which already supports it (gotcha #50) via the same `_ensure_creation_date_property()` helper. Every task created through standup Smart-Add (`/api/standup/smart-add`) or the "↗ push to board" button (`/api/standup/push-to-notion/<id>`) landed in Notion with a blank Creation Date, forever, even though a `due_date` was being set. Fixed by adding a `creation_date` kwarg to `create_task()` (writes via the existing helper, same as `update_task()`) and passing `today_ist()` from both of those two call sites (the ones that create tasks *from* the standup flow — other `create_task()` callers, e.g. client onboarding auto-tasks, were left untouched as out of scope for this fix).
    - **Verification:** pyflakes clean on `notion_store.py`/`ops.py` (no new warnings vs. baseline), `node --check` clean on `standup.html`'s script block, `import app` boots clean from `backend/`. Not verified against a live Notion write for this specific fix (no specific broken task was identified/available this session) — if a similar report recurs, check that the clicked task's `notion_id` is actually null in `standup_tasks` first, that's the trigger condition.

66. **`deleteClient()` in `projects.html` threw `ReferenceError: currentUser is not defined` on every click, for every client — 2026-07-30.** The function referenced a `currentUser` variable that never existed anywhere in the file; the real logged-in-user object is `user` (`const user = JSON.parse(localStorage.getItem("agency_portal_user")||"{}")`, line ~392), accessed elsewhere as `user.user_id`. Because the reference lived inside a template literal, the error threw before `fetch()` was even called — nothing ever reached the backend.
    - **Second bug found while fixing the first:** even with the variable fixed, `deleteClient()` unconditionally called `DELETE /api/clients/<int:client_id>` (`app.py`, SQLite-mode only, `<int:...>` route converter). For Notion-mode clients — the norm, per `notionMode`/`c.id = c.notion_id` — `clientId` is a UUID string, which never matches the `<int:...>` converter, so the request would 404 even after the ReferenceError was fixed. The correct Notion-mode endpoint is `DELETE /api/notion/clients/<string:notion_id>` (`routes/ops.py`, `notion_delete_client()` — archives the client's tasks then the client page itself).
    - **Fix:** `deleteClient()` now branches on the existing `notionMode` global (same pattern already used by `insertSheetRow()`/`duplicateSheetRow()`/etc. elsewhere in this file) to hit the right endpoint, and uses `user.user_id`.
    - **Verification:** `node --check` clean on the edited script block. Not exercised against a live client (destructive, no throwaway client available this session) — if client deletion still misbehaves, check whether `notionMode` was actually `true` at click time (it's set async in `load()` from `/api/notion/status`).

67. **Project Board Sheets — pasting more rows than existed below the cursor silently dropped the overflow, and Ctrl+Z did nothing right after a paste — both fixed 2026-07-30.** Two separate bugs in the paste handler added in gotcha #62.
    - **Bug 1 — paste never grew the table:** the paste loop (`document.addEventListener('paste', ...)`, `projects.html`) walked `curTr.nextElementSibling` to fill existing rows and stopped as soon as it ran out (`for (...&& curTr; r++)`), unlike real Excel/Google Sheets which auto-create rows for the overflow. It already surfaced a toast admitting this ("Add more rows to paste the rest") but that's not real Excel/Sheets parity, and pasting a batch that starts near/at the last row visibly looks like "only one line got pasted." **Fix:** the handler is now `async`; it first collects taskIds for however many rows already exist below the cursor, and for any shortfall calls a new `createBlankSheetRow(clientId)` helper (same POST body `insertSheetRow()` uses) in a loop to create exactly as many new rows as needed, once, then re-renders and resumes filling — by taskId via `querySelector`, not stale DOM refs, so it's robust to the re-render.
    - **Bug 2 — Ctrl+Z was a no-op immediately after pasting:** the keydown handler's undo branch only fired when `pendingRowDelete` was set (row-delete undo only, see gotcha #62) **and** `document.activeElement` wasn't itself editable. Right after a paste, focus is still sitting in the just-pasted contenteditable cell, so `isEditable` was `true` and the guard silently swallowed Ctrl+Z — and since the pasted values were written via direct DOM assignment (not `execCommand`), the browser's own per-cell undo history never learned about them either, so *nothing* undid the paste. **Fix:** added a parallel `pendingPasteUndo` state (8s window, same undo-bar UI) that snapshots each overwritten cell's prior value before `applyPastedCellValue()` runs, plus the ids of any brand-new rows created for Bug 1's overflow. `undoPaste()` restores the snapshotted values and **hard-deletes** the newly-created rows via the real `DELETE /api/notion/tasks/<id>` or `/api/sqlite/tasks/<id>` endpoint (unlike row-delete's deferred/soft undo — these rows are real backend records the instant they're created, there's no "not yet deleted" grace period to exploit). The keydown handler now checks `pendingPasteUndo` **before** `pendingRowDelete` and, deliberately, without the `isEditable` guard — that guard exists to let native in-cell undo win for real typing, but our paste writes aren't in that native undo stack in the first place, so gating on it just breaks Ctrl+Z outright for this case.
    - **Verification:** `node --check` clean on the edited script block. Not exercised against a live client this session (needs a real paste event + real Notion/SQLite writes to fully verify) — if paste/undo misbehaves again, check first whether the issue is in the new row-creation loop (`createBlankSheetRow`) vs. the value-fill loop vs. `undoPaste`'s cleanup, they're independent stages.

68. **Pasting a single multi-line cell's content (e.g. copying the whole "Idea" cell and pasting into "Content") got split one line per row instead of landing as one cell — fixed 2026-07-30, same session as gotcha #67.** Root cause: the paste handler's row-splitting (`text.split('\n')`) can't tell "one cell whose value happens to contain line breaks" apart from "multiple pasted rows" — both are just newline-separated plain text on the OS clipboard. Real Excel/Google Sheets solve this by quoting: when they write a cell containing a tab/newline to the clipboard as TSV, they wrap it in `"..."` (doubling any literal `"`), so a paste parser can tell a quoted field's internal newlines apart from a real row break. Our own `copy` handler wasn't doing that quoting, **and** it deliberately deferred to the browser's native copy whenever `window.getSelection()` was non-collapsed (`sel.isCollapsed` check, added in gotcha #62 to let a partial in-cell text selection copy normally) — but a **full**-cell selection (select-all/triple-click) is also non-collapsed, so it *also* fell through to native copy, which has no concept of TSV quoting and just hands over the raw text with literal `\n`s, indistinguishable from multi-row data.
    - **Fix, two parts:** (1) added `tsvQuoteField()` and use it in the `copy` handler for every field containing a tab/newline/quote, and (2) the single-cell bypass now compares `sel.toString()` against the cell's full `textContent` — only a genuine **partial** selection (the two differ) defers to native copy; a full-cell selection now goes through our quoting logic. (3) The paste handler no longer does naive `.split('\n')`/`.split('\t')` — added `parseTsvClipboard()`, a small quote-aware TSV parser (tracks `inQuotes`, doubles `""` as a literal `"`, only breaks fields/rows on unquoted tabs/newlines) matching the same convention Excel/Sheets use, so a quoted multi-line field now correctly lands in one cell.
    - **Why this didn't require touching `applyPastedCellValue()`:** cells already render with `white-space: pre-wrap` and are set via `td.textContent = value` (not `<br>`-based), so a value string containing real `\n` characters already displays as multiple visual lines in one cell — the bug was purely in how the clipboard text got serialized (copy side) and parsed (paste side), not in how a multi-line value is stored or displayed.
    - **Verification:** `node --check` clean. Not exercised against a real clipboard event this session — if a similar "content landed in the wrong cell/row" report recurs, check whether the source text reached `parseTsvClipboard()` already quoted (i.e. copied via our own `copy` handler) vs. raw from an external app/OS selection that doesn't know our quoting convention (Excel/Sheets *do* know it and quote correctly on their own; a plain-text editor or browser address bar would not).

69. **Sheets — added a per-row "Last Edited" indicator (who/when/which fields), Google-Sheets-lite, not full per-cell history — 2026-07-30.** User asked for Google Sheets-style edit tracking; scoped down to a per-row summary after confirming with them (full per-cell audit history would need a whole new log table). Deliberately does **not** use Notion's own built-in "Last edited by"/"Last edited time" property types — those would always attribute every edit to this app's single integration token/bot, never to whichever employee actually clicked Save, since all writes go through one shared `NOTION_TOKEN`.
    - **Storage:** one new Notion rich_text property, `"Last Edited"` (lazily auto-created via `_ensure_last_edited_property()` in `notion_store.py`, same pattern as Creation Date in gotcha #50 — Notion rejects a whole-page PATCH if it references a property that doesn't exist yet), storing a single pipe-delimited string `"{IST timestamp}|{editor name}|{comma-separated changed field labels}"`, parsed back out by `_parse_last_edited()`. For SQLite-mode clients, three plain columns instead (`last_edited_by`/`last_edited_at`/`last_edited_summary` on `tasks`, added via the standard try/except `ALTER TABLE` pattern in `db.py`).
    - **Backend wiring:** both `notion_update_task()` and `sqlite_patch_task()` (`routes/ops.py`) now read a `user_id` field out of the PATCH body (previously **not sent at all** — `saveSheetRow()` in `projects.html` had zero notion of who was editing), resolve it to a display name via the existing `_load_emp_names()` (same fix family as gotcha #63 — don't hardcode a name map here either), and build the changed-field summary straight from **which of the known keys are present in the request body** via a shared `_edit_summary_from_body()` / `_EDIT_FIELD_LABELS` map — no separate diff-tracking needed, the existing diff-only PATCH body IS the diff. Only writes/updates the last-edited fields when at least one real Sheets field actually changed (empty summary = skip), so a request that changed nothing tracked doesn't overwrite it with a no-op timestamp.
    - **Frontend:** `saveSheetRow()` now always sends `user_id: UID` alongside the diff. **First cut used a permanent 14th column** for this; user explicitly asked for it to be an on-demand affordance instead ("i dont think adding a column will be better just an option there to see the previous edits"), so it's a small 🕓 icon in the sticky row-0 cell (next to the row number and delete icon) that only renders when `t.last_edited_by` is set — no icon, no layout change, for rows never edited. Hovering shows the full detail via `title`; clicking calls `showLastEditedInfo(taskId, clientId)`, which looks the task up in `allData` and surfaces `"{name} · {relative time} ({absolute timestamp}) · Changed: {fields}"` via the existing `toast()` (click instead of hover-only so it also works on touch devices). Updated **optimistically** in the same "patch the DOM in place, don't re-render the whole table" style the rest of `saveSheetRow()` already uses (to avoid focus/scroll loss): it creates the icon element on first edit if not already present, via a small client-side mirror of the backend's field-label map (`EDIT_FIELD_LABELS`) — the next full data reload's real value from the backend is authoritative (mainly matters if the optimistic write actually failed server-side, which the existing `res.ok` check already prevents this block from running on).
    - **Verification:** pyflakes clean on `notion_store.py`/`ops.py`/`app.py`/`db.py` (no new warnings vs. baseline), `node --check` clean on the edited `projects.html` script block, `import app` boots clean. Not exercised against a live client this session (would need a real Sheets edit + real Notion/SQLite write) — if the 🕓 icon never appears after an edit, check first whether `saveSheetRow()`'s PATCH actually included `user_id` and whether the field that changed is one of the seven keys in `_EDIT_FIELD_LABELS`/`EDIT_FIELD_LABELS` (anything outside that list, if ever added, needs a label added to *both* copies or it silently won't show up in the summary).

70. **Tab-close checkout — built, shipped, then reverted after a production incident. Currently NOT deployed.** User wanted closing the Lumina browser tab to count as an instant employee checkout. Built as: `POST /api/attendance/ping` (`routes/attendance.py`, self-heals a checkout within a 90s window) + `frontend/auth.js` sending `navigator.sendBeacon()` on `pagehide`, plus a periodic 60s safety-net ping and a ping-on-load. Spec: `docs/superpowers/specs/2026-07-31-tab-close-checkout-design.md`. Shipped alongside gotcha #72 (Sheets Edit History panel) in the same deploy — see gotcha #71, that combined deploy broke login in production and both features were rolled back together via `git revert`. **As of this writing the revert stands** — none of the ping/pagehide code is in the codebase. Gotcha #72 was separately re-applied after the rollback (it wasn't implicated); tab-close-checkout was not. If asked to "check the tab-close checkout feature" or "why doesn't closing the tab check me out", the honest answer is it isn't live — check `git log --oneline --all | grep -i "tab-close\|attendance/ping"` for the build/revert history before assuming it's just broken.

71. **Production incident, 2026-08-01ish: login hung ~30s then 500'd for every employee, right after deploying tab-close-checkout + Sheets Edit History panel together.** Diagnosed via curl timing (`/api/health` fast, `/api/attendance/ping` fast, `/api/auth/login` consistently ~30.4-30.6s then 500) plus code reading: `sqlite3.connect(DB_PATH, timeout=30.0)` in `db.py::get_connection()` matches the hang duration exactly. Working hypothesis — **not fully confirmed, no server log access to prove it** — is a gevent-worker/sqlite3 C-extension blocking-call incompatibility, tipped over by tab-close-checkout's write-volume increase (`pagehide` fires on every internal navigation site-wide, not just real tab closes, so the ping endpoint got hit far more than intended). User explicitly authorized an emergency rollback after being shown the evidence; fixed via `git revert` (not reset) of the 5 relevant commits, pushed, verified login recovered (200, ~0.4s). **If login ever hangs ~30s again in production, check `get_connection()`'s busy-timeout and recent write-heavy frontend changes first** — this is the second-most-likely root cause after a straightforward code bug, and the 30s number is a strong fingerprint.
    - **Post-mortem lesson:** any frontend change that fires a write on a high-frequency browser event (`pagehide`, `visibilitychange`, `beforeunload`, scroll/resize handlers) needs to be checked for "does this fire way more often than the feature name implies" before shipping — `pagehide` firing on *every* internal link click (not just actual tab close) was the actual multiplier here, not the SQLite timeout itself.

72. **Sheets "Edit History" rollup panel — shipped, reverted with #70/#71's rollback, then explicitly re-requested and reshipped on its own, superseded by #76.** Read-only feature (no write-volume risk, unlike #70), so once the user asked to redeploy it alone post-incident, it was re-applied via `git revert` of the two revert-commits, verified `attendance.py`/`auth.js` still had zero ping/pagehide code (confirmed by grep), and pushed clean. Originally a toolbar dropdown button (`🕓 Edit History`) listing latest-edit-per-row (editor + relative time + which fields changed), reading from the per-row `last_edited_by`/`last_edited_at`/`last_edited_summary` fields added in gotcha #69. **This dropdown no longer exists** — gotcha #76 merged it into the single global Version History browser. Spec: `docs/superpowers/specs/2026-07-31-sheets-edit-history-panel-design.md`.

73. **Sheets — false-positive "edited" marking on blur with no real change, root cause found 2026-08-03.** User reported clicking into a row's Status cell without changing it still marked the row as edited (spurious 🕓 icon / edit-log entry). Root cause: `applySheetFields()` (`projects.html`) compared the `<select>` element's value — always normalized to `lowercase_underscore` (e.g. `"in_progress"`) — directly against `taskObj.status`, whose stored casing could be anything (`"In Progress"`, `"IN_PROGRESS"`, etc., depending on how it was last written). Any row whose stored status wasn't already in the exact normalized form permanently mismatched on every blur, regardless of whether the user actually changed the dropdown. Fixed with:
    ```javascript
    const normTaskStatus = (taskObj.status || "").toLowerCase().replace(/ /g, '_');
    if (normTaskStatus !== status) changedPayload.status = status;
    ```
    **Known unresolved caveat, same bug family, not fixed:** a Content/Notes false-positive was also seen once (on "Post 4") — likely the composite `description` string (built by joining non-empty `Content:`/`Idea:`/`Scripts:`/etc. segments with `" | "`, then regex-parsed back apart, see the parsing block above `buildSheetRecordsForClient()`) drifting from `taskObj.description` in some formatting edge case, but it wasn't root-caused — no reproducible mismatch was provable statically, and per systematic-debugging discipline it wasn't guess-patched. If this recurs, get an exact repro (which field, what was typed, was the row previously edited by a different code path e.g. restore) before touching the comparison logic.

74. **Sheets — per-row Version History overlay with restore, built 2026-08-03, Google-Sheets-style but scoped to one row.** User showed a Google Sheets "Version history" screenshot and asked for the same: see exactly what changed, when, by whom, with the ability to restore. Explicitly scoped to **per-row only** (not whole-sheet) during brainstorming — reconfirmed again in gotcha #75 — because this is a shared multi-editor sheet and a whole-sheet restore would silently discard other people's concurrent edits to unrelated rows. Spec: `docs/superpowers/specs/2026-08-03-sheets-row-version-history-design.md`.
    - **New table**, `sheet_edit_log` (`backend/db.py`): `id, task_id, client_id, editor_name, edited_at, snapshot` — `snapshot` is a full JSON dump of all 12 raw Sheets fields (`creation_date, due_date, title, type, content, idea, scripts, caption, link, myNotes, assigned_to, status`) per edit event, **not a diff**. Written regardless of whether the task itself is Notion- or SQLite-backed, since it's purely local bookkeeping.
    - **New routes** (`backend/routes/ops.py`): `POST /api/sheets/tasks/<task_id>/log-version` (append one snapshot), `GET /api/sheets/tasks/<task_id>/versions` (that row's full history, newest first).
    - **Frontend refactor:** `saveSheetRow()` was split into a thin DOM-reading wrapper that calls the new shared `applySheetFields(taskId, clientId, fields, cellElem)` — `fields` is the same 12-key raw shape, `cellElem` is nullable (restore flows pass `null`, there's no specific clicked cell). `applySheetFields()` does the diffing against `taskObj`, PATCHes Notion/SQLite, updates the row background + `🕓` last-edited icon, and fire-and-forgets a `log-version` POST — a missed log write must never block or fail the save that already succeeded.
    - **Overlay** (`#row-history-overlay`, opened by the row's `🕓` icon via `openRowHistory(taskId, clientId)`): sidebar lists every version for that row, day-grouped ("Today"/"Yesterday"/date); selecting one renders a 12-field diff view (`RH_FIELD_LABELS` maps raw keys → display labels) highlighting which field changed vs. that row's immediately-prior version. `restoreRowHistoryVersion(snapshot)` calls `applySheetFields(taskId, clientId, snapshot, null)` then `writeSnapshotToRowCells(tr, snapshot)` to sync the live DOM (restore changes data the DOM isn't currently displaying, unlike a normal typed edit).
    - **Limitation, by design, not a bug:** there is no history before this feature shipped — a row never edited since 2026-08-03 has zero versions. Not fixable (no data was ever captured), and not meant to be.

75. **Sheets — global whole-sheet Version History browser, built 2026-08-03, same day as #74.** User showed a second Google Sheets screenshot — the *global* version history view (single timeline across every row/editor, clicking a version opens the full sheet grid as it looked at that moment with the changed cell highlighted) — and asked for the same. Spec: `docs/superpowers/specs/2026-08-03-sheets-global-version-browser-design.md`. **Restore was explicitly re-confirmed to stay per-row-only** even in this whole-sheet-browsing view, same reasoning as #74.
    - **New route:** `GET /api/sheets/clients/<client_id>/versions` (`routes/ops.py`) — same `sheet_edit_log` table as #74, queried by `client_id` instead of `task_id`, returns every edit event for the client newest-first. No new table.
    - **`buildSheetRecordsForClient(client)`** was extracted out of `renderClientSheets()`'s previously-inline records-parsing `.map()` (exact same logic) so both the live sheet render and the new global browser share one source of truth for "what does this row currently look like."
    - **Reconstruction logic** (`ghRowFieldsAtVersion(taskId, uptoEditedAt)`): for a selected version's timestamp, walks that row's `sheet_edit_log` entries (versions array sorted DESC) for the latest one at-or-before that time; falls back to the row's *current* live value (`ghFieldsFromCurrentRecord`) if the row has no snapshot before that time — inherent limitation, same as #74, not a bug.
    - **Highlight logic** (`ghChangedFieldFor(version)`): diffs a version's snapshot against that same row's immediately-prior version to find the one changed field, reusing the diffing idea from #74's per-row overlay.
    - **Restore** (`restoreSheetHistoryVersion`): calls `applySheetFields(version.task_id, ghState.clientId, version.snapshot, null)` — only ever the one row the selected version actually changed, never any other row visible in the grid.
    - Overlay id `#sheet-history-overlay`, opened via `openSheetHistory(clientId)`. At ship time this lived behind a *second*, separate toolbar button (`🕐 Version History`) next to the old Edit History dropdown from #72 — merged into one button in gotcha #76.

76. **Sheets — merged the Edit History dropdown (#72) and global Version History browser (#75) into one toolbar entry point, fixed highlight visibility, 2026-08-03.** User feedback after trying #75: two history buttons side by side was confusing, and selecting a past version didn't visibly highlight anything.
    - **Merge:** removed the `🕓 Edit History` dropdown button, its menu markup, and the now-dead `renderEditHistoryList()`/`toggleEditHistoryMenu()` functions plus their outside-click-to-close handler entirely. The remaining single toolbar button keeps the familiar `🕓 Edit History` label but now opens `openSheetHistory()` — the full global browser from #75, a strict superset of what the dropdown showed (per-edit rollup *and* full-grid drill-in *and* restore).
    - **Highlight bug — root cause was visibility, not logic.** Traced `ghChangedFieldFor()`/the `gh-highlight`/`gh-highlight-row` CSS classes and confirmed the highlight logic itself was correct and firing — the actual problem was that `renderSheetHistoryMain()` never scrolled to the highlighted row, so on a sheet with many rows the highlighted one was frequently below the fold and looked like nothing was highlighted at all. Fixed two ways: (1) `highlightedRow.scrollIntoView({ block: 'center' })` after every render in `renderSheetHistoryMain()`, and (2) strengthened the CSS itself (`.gh-table td.gh-highlight` now adds an inset `box-shadow` ring in `--acc`; `.gh-table tr.gh-highlight-row td` gets a visible top/bottom border) so it's obvious even without scrolling.
    - **The Content/Idea column mismatch reported in the same session turned out to be a false alarm, not a bug.** After the scroll fix above, the user re-checked and Post 4's Content cell genuinely did show "hello" — the earlier screenshot was almost certainly the row scrolled off-screen combined with stale on-screen data, not a real field swap. No fix was needed; the code path (`saveSheetRow()`/`applySheetFields()`/`writeSnapshotToRowCells()`) was correct all along, confirmed by re-reading it end to end.
    - **Real, separate bug found right after: neither history overlay could highlight the changed cell for a row's first-ever logged edit.** Both `ghChangedFieldFor()` (global browser) and `renderRowHistoryMain()` (per-row overlay) determined "what changed" by diffing a version's snapshot against that same row's immediately-prior version — which is `undefined` for the very first version ever logged for a row (there's nothing older to compare against), so both silently fell back to "nothing changed," even though the edit that created that very version obviously changed something. This is exactly the case a brand-new row/first-ever-edit always hits, so it looked like cell highlighting "just didn't work."
    - **Fix: capture the changed-field list at save time instead of reconstructing it later by diffing.** `applySheetFields()` now builds an `oldRaw` snapshot of the row's 12 fields *before* mutating `taskObj` (parsing `taskObj.description` via the newly-extracted `parseSheetNotes(notes, existingCreationDate)` helper — pulled out of `buildSheetRecordsForClient()` so both places share one parser instead of two copies), diffs it against the new `fields`, and sends the resulting `changedRawFields` array as `changed_fields` in the `log-version` POST body. New nullable `sheet_edit_log.changed_fields` column (`db.py`, standard try/except `ALTER TABLE`, stores a JSON array) carries it through `log_sheet_version()`/`list_sheet_versions()`/`list_client_sheet_versions()` (`routes/ops.py`). Both overlays now read `version.changed_fields` directly when present, and only fall back to the old sibling-diff logic for versions logged before this change shipped (which have `changed_fields = null`). `ghChangedFieldFor()` now returns an array (supports highlighting multiple changed cells in one version, not just one).
    - **Verification:** `node --check` clean on the edited `projects.html` script block, pyflakes clean on `ops.py`/`db.py` (no new warnings vs. baseline), `import app` boots clean. Not exercised against a live edit this session — if a cell still doesn't highlight after this fix, check first whether `sheet_edit_log.changed_fields` actually has the column (server restart needed to run the `ALTER TABLE` migration) and whether the version's `changed_fields` came back non-null from the API before assuming the diff logic itself is wrong again.

77. **Standup "Velocity Tracking" chart (`standup.html`) — added a "This Month" range + an always-visible month-to-date completed/overdue summary, 2026-08-03.** User wanted to track, from the 1st of the month, a running total of tasks completed vs. gone overdue, visible at month-end. The existing chart only offered 7/14/30-day windows and had no summary number — you'd have to eyeball-sum the daily bars, which is also **not a valid way to get a monthly overdue total**: see the caveat below.
    - **Backend (`routes/ops.py`):** `GET /api/standup/velocity` now accepts `range=month` as an alternative to `days=N` (computes `since` as the 1st of the current calendar month) — additive, `days` behavior unchanged. New route `GET /api/standup/velocity-summary?user_id=` returns **month-to-date totals**, always for the current calendar month regardless of the chart's own range selector: `{completed, overdue, month_start}`.
    - **Why the overdue count can't just be summed from the existing daily chart data:** a `standup_tasks` row is re-inserted as a **new row every day** a task stays pending (see the carry-over `INSERT` logic elsewhere in this file) — each with the *same* `carried_from` value, which is set once to the date the task first went unfinished and never changes on subsequent carries. So the existing `/velocity` endpoint's daily `carried` count is a snapshot-per-day, and summing it across a month counts the same overdue task once for every day it remained outstanding. `velocity-summary` avoids this by counting `COUNT(DISTINCT user_id || '|' || title || '|' || carried_from)` instead of raw rows — one count per task lineage, not per day. `completed` doesn't need the same treatment since a task is only ever marked `status='done'` on the one row/day that happened, and done tasks aren't re-inserted by the carry-over logic.
    - **Frontend:** new `#velocity-summary` strip above the chart, populated by `loadVelocitySummary()` (called from `loadVelocity()` on every scope/range change, but itself ignores the range selector — always current month). Respects the existing `My Tasks`/`Team` scope dropdown. `"This Month"` was added as a 4th `<option>` on the existing days `<select>`; `loadVelocity()` sends `range=month` instead of `days=N` when that option is selected.
    - **Scope decision, deliberate:** no month-picker (only ever "current month," no browsing past months) — matches exactly what was asked for (a running month-to-date tally) without building a feature nobody requested yet. If "let me see July's totals" comes up later, `velocity-summary` would need a `month=YYYY-MM` param added.
    - **Verification:** `node --check` clean on `standup.html`'s script block, pyflakes clean on `ops.py` (no new warnings), `import app` boots clean, both new endpoints curl-tested against a local dev server (`{"completed":0,"overdue":0,"month_start":"2026-08-01"}` and `{"velocity":[]}` — empty because the local dev DB has no standup data, not an error). Not exercised against real production standup data or in-browser this session.
    - **Follow-up same day: weekends excluded from the chart.** User pointed out Sat/Sun were showing large "Carried Over" spikes even though nobody works those days — expected, since the auto-carry-over logic (gotcha #8) runs whenever anyone loads `/api/standup/my-tasks`, regardless of day of week, so a weekend's row count reflects whatever was pending, not real weekend activity. Fixed by adding `AND strftime('%w', date) NOT IN ('0','6')` (SQLite: 0=Sunday, 6=Saturday) to both `get_velocity()` queries in `routes/ops.py` — weekend dates no longer appear in the chart at all. Deliberately scoped to the **chart only**: `get_velocity_summary()`'s month-to-date totals were left untouched (still count every day, weekends included) since the user's complaint was specifically about the graph being visually noisy, not about the totals being wrong — if weekend rows also need excluding from the monthly totals, that's a separate follow-up, not assumed here.
    - **Follow-up: card moved to full page width, per-employee filter added, weekday shown under each date.** The card originally lived inside `standup.html`'s 2-column `.layout` grid (sharing half the page with Team Standups), too narrow once weekends started getting skipped and days needed more horizontal room — moved out to its own full-width row directly under `.layout`, chart height bumped 220→260px to match the wider aspect ratio.
      - **Per-employee filter:** the `velocity-scope` dropdown only ever had "My Tasks" / "Team" (aggregate) — no way to see one specific teammate's velocity. `populateVelocityEmployeeOptions()` appends an `<optgroup label="Individual">` with one `<option>` per `window.EMPLOYEES` entry (value = employee id, same id space "My Tasks" already used) once `loadEmployees()` resolves. `velocityUserIdFor(scope)` centralizes the three-way mapping (`"team"` → no filter, `"me"` → `UID`, anything else → that literal employee id) so `loadVelocity()`/`loadVelocitySummary()` don't duplicate the branching — no backend change needed, `/api/standup/velocity(-summary)?user_id=` already accepted any employee id, it just had no UI path to reach it for anyone but yourself.
      - **Weekday under each date:** Chart.js renders an array-valued label as multiple tick lines, so `labels` changed from `d.date.slice(5)` (a string) to `[d.date.slice(5), velocityWeekdayShort(d.date)]` — no Chart.js config changes needed beyond that.
      - **Verification:** `node --check` clean on the edited script block, div-tag balance re-checked after the layout move (80 open / 80 close). Not exercised in-browser this session.

78. **Sheets — "Save failed" on long Content/Idea/Scripts text, root-caused and fixed 2026-08-06.** User reported Sheets edits silently failing to save ("Save failed ✕", no useful detail). Investigated live against production rather than guessing: `GET /api/notion/schema-check` confirmed Notion was reachable/configured/all required properties present (ruled out a config/outage cause); a minimal test `PATCH /api/notion/tasks/<id>` (no-op `creation_date`) against a real task succeeded, ruling out a total endpoint outage; a second test PATCH with a 2319-character `submission_note` reproduced the exact failure (`500 {"error":"Failed to update task in Notion"}`).
    - **Root cause:** Notion's API rejects a page-update request outright if any single `rich_text` object's `content` exceeds 2000 characters — and `_text()` (`notion_store.py`) always wrote the entire composite Sheets Notes string (Content + Idea + Scripts + Caption + Notes joined with `" | "`, see the parsing block in `projects.html`'s `buildSheetRecordsForClient`/`applySheetFields`) as a single text object. Any row with a longer AI-written script (multi-slide carousel scripts routinely run past 2000 chars) blew the limit. Worse: Notion fails the **entire** PATCH on this, not just the offending property — so a request that also carried a status/title/due_date change in the same diff-only payload (`saveSheetRow()`'s normal pattern) lost all of it, not just the long text field, with no indication in the UI of why.
    - **Fix:** `_text()` now chunks the string into multiple `rich_text` array entries (Notion's own documented way to store longer text) instead of one oversized block. `_get_text()` (the reader) already did `"".join(t.get("plain_text","") for t in items)` over the whole array — so this is a fully symmetric fix requiring no read-side change and no data migration; existing short values still produce a single-entry array exactly as before.
    - **Verified live, fully reversible:** re-ran the same 2319-char PATCH against the same real task (`396cacb2-...`, "Post 13") post-deploy → `200 {"success":true}`; confirmed via `/api/notion/dashboard` the full 2319 characters round-tripped correctly (chunked write, joined read); then restored the task's `submission_note` to its exact original value and re-confirmed via the same dashboard read. Net effect on production data: zero.
    - **If "Save failed" recurs:** check the row's combined Content+Idea+Scripts+Caption+Notes length first (2000-char chunks now handle arbitrarily long text, but a single *field* — e.g. one giant Scripts value alone — still can't exceed Notion's separate per-property array-length/page-size ceilings, which are far higher and haven't been hit in practice). If it's not a length issue, the generic `{"error":"Failed to update task in Notion"}` swallows the real Notion error text in `update_task()`'s broad `except Exception: logger.exception(...); return False` — there's no server log access from this session, so narrowing further needs either log access or another live reproduction like the one used here.

79. **Two unrelated Agrius incidents surfaced together, 2026-08-06 — one false alarm (tasks not deleted, just invisible to the app) and one real data-loss bug (fixed).** User reported ~17 Sheets tasks "gone" after an Excel import attempt + clicking Add Row + refreshing. Investigated live rather than guessing, per systematic-debugging — found two completely separate things:
    - **False alarm — 43 pre-existing "Agrius" tasks were never deleted, they were never visible to this app in the first place.** `GET /api/notion/tasks` (unfiltered) turned up 47 tasks with "Agrius" in them; 43 had a **blank `Client ID` property**. `notion_store.list_tasks(client_notion_id=...)` filters strictly on `Client ID` (`{"property": "Client ID", "rich_text": {"equals": ...}}`) — a task whose Client ID was never set (these all predate the app, titled things like "Agrius Sheet", "Agrius Pinned Post", dated back to 2026-06-29, clearly created by hand directly in Notion rather than through this app's `create_task()`, which always sets it) is 100% invisible to every view in this app — Sheets, Task List, Kanban, dashboard — while fully intact and visible in Notion.so. Nothing to fix here per se (not a bug, just an app/Notion linkage gap for hand-created pages) — **flagged as a possible future feature**: a repair tool that back-fills `Client ID` by fuzzy-matching `Customer Name` text to a real client, with a review step before applying (the matches in this dataset included "AGrius" and "Agrius AI" as distinct casings/variants — a human needs to confirm those are/aren't the same client before any bulk assignment). Not built this session — deferred, no request to build it yet.
    - **Real bug, found and fixed — Ctrl+Z paste-undo had zero focus scoping, silently deleting freshly-imported rows on an unrelated keystroke.** The other ~5+ tasks (titled `[Type] Post N`, matching the Sheets auto-numbering convention) were confirmed by the user to be sitting in **Notion's own Trash** (screenshot showed `[Carousel] Post 3`, `[Reel] Post 15/17/18`, `[Static] Post 16`, restorable via Notion's own ↩ icon — not reachable or restorable via this app's API). Root cause: the `keydown` handler's Ctrl+Z branch for `pendingPasteUndo` (added in gotcha #67) had **no check on what element was focused** — any Ctrl+Z press anywhere on the page, for any reason, within the 8-second undo window after a Sheets paste would call `undoPaste()`, which hard-deletes every row that paste had freshly created via the real `DELETE /api/notion/tasks/<id>` endpoint. A user pasting an Excel import, then pressing Ctrl+Z moments later to fix an unrelated typo in a completely different cell (or anywhere else on the page), would silently wipe the import with zero visible connection between "I undid a small typo" and "my whole import vanished" — exactly matching the user's own description ("imported it but it didn't get add[ed]").
      - **Fix:** added `isFocusInPastedRows()` — checks whether `document.activeElement`'s closest `tr.sheet-row` has a `data-task-id` that's actually part of the pending paste (`pendingPasteUndo.cellSnapshots` or `.newTaskIds`). The keydown branch now only fires `undoPaste()` when that's true, preserving the original intent (Ctrl+Z works immediately while still in the just-pasted cell, bypassing native undo history which never learned about the programmatic write) without the global hijack. The visible "Undo" toast button (`showUndoBar`) is unaffected — its `onclick` calls `undoPaste()` directly with no focus check, so it remains the reliable intentional-undo path from anywhere for the full 8s.
      - **Verification:** `node --check` clean on the edited `projects.html` script block. Not exercised against a live paste this session (needs a real paste event to fully verify) — if rows still vanish unexpectedly after a paste, confirm first whether `isFocusInPastedRows()` is actually being reached (i.e. some other delete path — manual 🗑️ row-delete, or `pendingRowDelete`'s own Ctrl+Z branch, which already had a proper `isEditable` guard and was not touched) before assuming this fix regressed.
    - **Also reworked `importClientSocialCSV()` per explicit user request** ("in the future when i click import dont use claude for it... just sort them by [date] and add them below dont delete the previous one"): removed the `/api/social-media/auto-fill` (Claude/Sonnet) step entirely — the Excel/CSV columns already map 1:1 to what `/api/clients/<id>/auto-tasks` expects, so there was nothing for it to clean up, and it was a pure extra failure point (plus cost). Parsed rows are now sorted by `postDay` (falling back to `creationDate`) via plain string comparison before sending — correct for ISO `YYYY-MM-DD` values (which is what the Excel path always produces, converting `Date` cells via `.toISOString()`), **not guaranteed correct for a raw CSV with non-ISO date text** — acceptable limitation, not fixed, since the report was specifically about the Excel path. Confirmed via code review (not just this fix) that `/api/clients/<id>/auto-tasks` (`auto_generate_tasks` in `app.py`) only ever calls `notion_store.create_task()` in a loop — it has no delete/replace logic at all, so "don't delete the previous ones" was already true before this change; the toast now also surfaces the real `tasks_created` count and any backend `error` field instead of a generic success message regardless of outcome. The backend `/api/social-media/auto-fill` route (`app.py::auto_fill_social_media`) is now unreferenced by any frontend file (confirmed by grep) but was deliberately **left in place, not deleted** — it's a documented, real feature (bulk-fills empty caption/script fields via Sonnet) that just wasn't wired into this particular import flow; removing a working endpoint wasn't part of what was asked.

80. **`emp010` reassigned from Anagha to Sid, 2026-08-06.** `config/employees.json` — same `id` kept (`emp010`) so any existing task/standup history tied to that id (assignments, standup rows, etc.) stays linked rather than orphaning. Fields changed: `name: "Sid"`, `role: "1Founder"` (literal string, given as-is by the user — not a typo for "Founder"), `pin: "0000"` (duplicate PIN is fine — `routes/auth.py::login()` matches by `user_id` first, *then* checks that specific employee's PIN, so two employees can share a PIN with zero collision risk; Noorish/emp009 already uses `0000` too). `tone_profile`/`department` reset to match the existing Founder-role entry's style (Kshitij/emp004). Verified live post-deploy: `POST /api/auth/login {"user_id":"emp010","pin":"0000"}` → `200 {"success":true,"user":{"name":"Sid",...}}`. No code changes needed — confirmed via gotcha #63's `_load_emp_names()` fix that nothing hardcodes employee names anymore, and grep for "Anagha" across the repo returned zero hits before making this change.

81. **Team Standups history-date picker silently snapped back to today a few seconds after picking a past date — root-caused and fixed 2026-08-06.** `loadTeamStandups(dateStr = "")` (`standup.html`) treated a missing argument as "show today" and reset `#history-date`'s value accordingly (lines near the top of the function: `if (!dateStr && ...) { history-date.value = today }`). Three call sites invoke it with **zero arguments** — `setInterval(loadTeamStandups, 30000)` (the 30s auto-refresh), plus `updateBlocker()`/`updateTaskTitle()` after a personal-task edit — all of which used to mean "reset to today," clobbering whatever past date the user had manually selected in the picker. Fixed by changing the default so a truly *omitted* argument (`dateStr === undefined`) now reads `#history-date`'s current value instead of hardcoding today; the picker's own `onchange="loadTeamStandups(this.value)"` still passes an explicit string (even `""` if cleared) and is unaffected, so clearing the picker by hand still correctly falls back to today. `node --check` clean; not exercised against a live picker interaction this session.

82. **Sheets — added row-hide (display-only, not a data change), 2026-08-06.** User wanted a way to keep old/previous-month rows in the sheet without them cluttering the top, needing to scroll far to reach the current month — explicitly asked for hide-only (data stays intact, and Calendar/Kanban/Task List must keep showing hidden rows). Implemented as a pure client-side filter local to `renderClientSheets()`, same `localStorage`-per-client pattern as the existing hidden-columns feature (`sheetsHiddenRows_<clientId>`, a JSON array of task ids via new `loadHiddenRows()`/`saveHiddenRows()`). Since Calendar/Kanban/Task List all read `allData` directly and never call this filter, "still show everywhere else" required no extra work — it's a natural consequence of scoping the filter to one function.
    - **Entry points:** right-click a row → new "Hide row" context-menu item (`showRowContextMenu`); or check multiple rows and click "Hide" in the existing bulk-action bar (new `bulkHideChecked()`, sits next to the bulk status-select/Delete/Clear buttons added in gotcha #62).
    - **Un-hiding:** a new "Hidden (N)" toolbar button (next to Edit History) opens a dropdown listing every hidden row by title with a per-row "Unhide" link and an "Unhide all" bulk action — mirrors the existing Columns dropdown's structure exactly.
    - **Emoji follow-up, same day:** first cut used a 🙈 emoji on the new buttons/menu/toast, inconsistent with this app's plain-text convention elsewhere in `projects.html`'s Sheets toolbar — user said "dont use emojis" immediately after seeing it described; fixed by stripping the emoji from all four new strings, reverted to plain text labels ("Hide", "Hidden (N)", "Hide row"). Scoped to just what was just added, not a sweep of the rest of the app's pre-existing emoji-heavy buttons (📥 Import, ➕ Add Row, ☰ Columns, etc. all remain as-is) — those weren't part of this change and removing them wasn't asked for.
    - **Verification:** `node --check` clean; whole-file `<div>`/`</div>` regex count showed a 2-tag gap both before and after this edit (193/195 → 198/200, same delta) — confirmed pre-existing noise from `<div` substrings inside unrelated JS string literals elsewhere in this large file, not a real markup imbalance introduced here (the new block's own tags were hand-verified balanced: 5 opens, 5 closes). Not exercised against a live client's Sheets tab this session.
    - **Superseded same week — see gotcha #83:** the row-hide state described above was per-browser `localStorage`, which turned out wrong the moment a second employee opened the same client (see #83).

83. **Sheets — row-hide moved from per-browser `localStorage` to a shared server table, and a Month filter added, 2026-08-10.** User reported hiding a row as Palak was still visible to Vidit — exactly the localStorage-per-browser limitation flagged (but not caught) when gotcha #82 shipped: `sheetsHiddenRows_<clientId>` lived in each employee's own browser, so "hidden" was never actually shared. Same session, user also asked for a way to view the Sheet one month at a time instead of scrolling past old rows — a second, complementary fix to the same underlying "too many old rows visible" complaint from #82.
    - **Shared hide/unhide:** new SQLite table `sheet_hidden_rows` (`task_id` PK, `client_id`, `hidden_by`, `hidden_at`) in `db.py`, works for both Notion- and SQLite-backed tasks the same way `sheet_edit_log` (gotcha #74) does — `task_id` is whatever id the row already uses (`notion_id` or SQLite id), no FK, presence in the table = hidden. Four new routes in `routes/ops.py`: `GET /api/sheets/hidden-rows` (every client's hidden rows in one shot — powers a page-load cache, not a per-client fetch, since expanding a client card must never block on a network round-trip), `POST /api/sheets/tasks/<id>/hide`, `POST /api/sheets/tasks/<id>/unhide`, `POST /api/sheets/clients/<id>/unhide-all`.
    - **Frontend:** `projects.html`'s `load()` now calls `loadAllHiddenRowsFromServer()` once, populating `hiddenRowsCache`/`hiddenRowsMeta` (clientId → Set/meta-map). `loadHiddenRows(clientId)` reads the cache synchronously (unchanged call sites, `renderClientSheets()` doesn't need to become async) while `hideSheetRow()`/`unhideSheetRow()`/`unhideAllSheetRows()`/`bulkHideChecked()` optimistically update the cache + re-render, then fire the real POST in the background (toast on sync failure, matching the existing "Save failed"-style pattern elsewhere in this file). The "Hidden (N)" dropdown now also shows a `title` tooltip with who hid the row and when, using `hiddenRowsMeta`.
    - **Month filter:** new per-client `<select>` next to the search box, options built from every distinct `due_date` (Post Day) month present in that client's rows (`allMonths`, computed before any filter so removed/hidden rows don't shrink the dropdown). Selection stored in `localStorage` (`sheetsMonthFilter_<clientId>`) via `loadMonthFilter()`/`saveMonthFilter()` — deliberately kept **per-browser**, unlike row-hide, since it's a personal view preference (like column visibility, gotcha #62) rather than shared data; there's no correctness bug in it being local the way there was for hiding.
    - **Verification:** `node --check` clean on the extracted script block, pyflakes clean on `ops.py`/`db.py` (matches known pre-existing warning baseline), `import app` boots clean. Post-deploy: `curl /api/sheets/hidden-rows` returns 200. Not exercised against a live two-browser hide/unhide this session — if a hidden row still doesn't sync across users, check first whether `loadAllHiddenRowsFromServer()` actually resolved before the client card was expanded (network race), not the backend logic.

84. **Native `<input type="date">` pickers forced to dd/mm/yyyy app-wide, 2026-08-10.** User screenshot showed the Sheets Creation-Date/Post-Day columns displaying inconsistent formats (one field showing the raw `dd-mm-yyyy` placeholder, an adjacent one showing an actual filled `10-08-2026`). Root cause: a native `<input type="date">`'s displayed format (not its stored value, which is always ISO `YYYY-MM-DD` regardless) is rendered by the browser according to the OS/browser locale — uncontrollable via CSS, and inconsistent machine-to-machine. Fixed by adding `lang="en-GB"` to every `<input type="date">` in the app (Chrome/Edge read an element's own `lang` attribute, or its nearest ancestor's, to pick the display format for date/number inputs) — forces dd/mm/yyyy everywhere without touching how any date value is read, written, or stored. Touched: `projects.html` (Sheets Creation Date/Post Day, quick-task/edit-task due date, new-deadline picker), `add-tasks.html` (deadline, Sheets postDay/creationDate columns, task-panel due date), `standup.html` (post-date picker, team-standups history-date picker), `client-admin.html`, `client-onboard.html`. Two JS `querySelector('input[type="date"]')` selector strings in `projects.html` got accidentally mangled by a blanket find-replace (became `input[type="date" lang="en-GB"]`, which happened to still match since the actual elements now carry that attribute too, but was fragile/wrong) — caught and reverted to the plain `type="date"` selector before committing. `node --check` clean on all 5 edited files; not exercised in a live browser this session — if a date picker still shows the wrong format, check whether that specific input actually got the `lang="en-GB"` attribute (a new date input added later without following this pattern would silently fall back to browser locale again).

85. **Sheets paste rewritten to always be single-cell, 2026-08-10 — fixes both "paste from anywhere should only fill that cell" and a knock-on "version-history Restore doesn't work after a paste" report.** The paste handler built across gotchas #62/#67/#68 treated ANY clipboard text containing a tab **or a newline** as an Excel-style multi-cell paste (`isMultiCell = /\t|\n/.test(text)`) and split it with `parseTsvClipboard()` across columns/rows, creating new rows via `createBlankSheetRow()` if the paste had more lines than rows existed below the cursor. Pasting a paragraph copied from a website, Word, or a chat app — no tabs, just line breaks — got smeared one line per row down the sheet instead of landing in the one cell being edited. Presented with two options (keep Excel-style multi-cell paste but gate it on a real tab character, vs. make every paste single-cell-only and point bulk entry at the existing Import CSV/Excel button), user chose **always single-cell** via `AskUserQuestion`.
    - **Why this also explains the separately-reported "Restore doesn't work"**: a multi-line external paste used to scatter its content across several different rows/tasks (sometimes newly-created ones), each independently saved and logged into `sheet_edit_log`. Opening *one* row's Version History and clicking Restore only ever touched that one row's own fragment of the original paste — never "undid the paste" as a whole, which looked like restore was broken. With paste now confined to a single cell, every paste is one clean, single-row edit with one clean version entry, so Restore behaves normally again.
    - **Fix, `projects.html`:** the `paste` listener now only intercepts `contentEditable` Sheets cells (date/select cells get native paste behavior untouched), writes the pasted text into the one targeted cell via the existing `applyPastedCellValue()`, and calls `saveSheetRow()` for that row same as a normal typed edit — no row creation, no column/row splitting. Also normalizes `\r\n`/`\r` → `\n` in the pasted text before writing it (text copied from outside the app almost always carries Windows line endings) — `\r` sitting invisibly in a cell's text node would otherwise survive the round trip to the backend and back, making a later diff (Restore comparing current value against an older snapshot) see a mismatch that isn't visually there. `pendingPasteUndo`/`undoPaste()`/`isFocusInPastedCell()` (renamed from `isFocusInPastedRows()`) simplified to single-cell shape — no more `cellSnapshots`/`newTaskIds` arrays, no more DELETE-the-newly-created-rows logic on Ctrl+Z, since paste can no longer create rows. Removed now-fully-dead `createBlankSheetRow()` and `parseTsvClipboard()`.
    - **Scope note:** the multi-cell **copy**-out handler (selecting a block of cells and Ctrl+C to paste into real Excel, gotcha #68's TSV-quoting) is untouched — this only changes how paste*ing into* the Sheets grid behaves. Bulk multi-row data entry still goes through the dedicated Import CSV/Excel button (`importClientSocialCSV()`, gotcha #79), which was never part of this paste-into-a-cell code path.
    - **Verification:** `node --check` clean; grepped for every removed function/variable name (`parseTsvClipboard`, `createBlankSheetRow`, `isFocusInPastedRows`, `cellSnapshots`, `newTaskIds`) to confirm zero dangling references. Not exercised against a live paste event or a live Restore click this session — if Restore still misbehaves after this fix, check first whether the row's `sheet_edit_log` snapshot itself predates this change (still carries scattered/partial data from the old multi-row paste bug) before assuming the new code is wrong.
    - **Immediate follow-up, same day — see gotcha #86:** user then reported clicking "Restore this version" in the global Version History browser did nothing at all. Root cause was unrelated to the paste behavior above: a CSS z-index bug meant `toast()` (both success and failure messages) was rendering completely hidden behind the full-screen history overlay, so restore may well have been working correctly the whole time with zero visible confirmation either way.

86. **Toast notifications invisible behind the Sheets Version History overlays — fixed 2026-08-10.** User clicked "Restore this version" in the global Version History browser (gotcha #75) and reported nothing happened. Investigated via code reading (not a live click, no browser access this session): `#toast` (`projects.html` CSS, ~line 129) was `z-index:200`, while `#sheet-history-overlay`/`#row-history-overlay` (gotchas #74/#75) are `z-index:10000`, full-viewport, with an **opaque** `background: var(--s1)` — not just higher in the stack but visually covering the toast entirely. `restoreSheetHistoryVersion()`/`restoreRowHistoryVersion()` both call `toast(...)` on every outcome (`'Nothing to restore -- row already matches this version, or the save failed.'` on a no-op/failure, `'Version restored'` on success) — for the **failure/no-op branch specifically**, the function `return`s immediately without closing the overlay, so that toast had zero chance of ever being seen; the success branch closes the overlay right after showing the toast, so it likely *was* visible for genuine successful restores, making the bug intermittent-looking depending on which case a user hit. Fixed by bumping `#toast` to `z-index:20000` — above both history overlays, a one-line CSS change with no logic touched. **Open question, not resolved:** whether the specific restore the user clicked was itself a no-op (the selected version's data already matched the row's current live state, so "Nothing to restore" was the technically-correct-but-invisible response) or a genuine save failure — indistinguishable from code alone. If a user reports a restore still not visibly applying after this z-index fix, that would point to a real second bug in `applySheetFields()`'s diff/save logic rather than a visibility issue, and is worth checking next.

87. **Google Sheets two-way sync built, 2026-08-11 — code complete, live end-to-end sync NOT yet verified (no service-account credential available this session).** User asked whether editing a real Google Sheet could stay in sync with a client's Sheets tab in both directions. Full design at `docs/superpowers/specs/2026-08-11-google-sheets-two-way-sync-design.md`, implementation plan at `docs/superpowers/plans/2026-08-11-google-sheets-two-way-sync.md`.
    - **New:** `backend/google_sheets_store.py` (raw Sheets API v4 client via a service-account `AuthorizedSession` from `google-auth` — added as a dependency, not the heavier `google-api-python-client`, consistent with `notion_store.py`'s raw-`requests` convention for external APIs; field-mapping helpers `_parse_sheet_notes`/`_build_sheet_notes` are a **Python port** of `parseSheetNotes()`/the `detailParts` join in `applySheetFields()` — must be kept in sync by hand if either side's composite-Notes format ever changes; task create/update/delete adapters branch on a per-link `is_notion` flag to support both Notion- and SQLite-backed clients; `push_task_to_sheet()`/`reconcile_sheet_rows()` are the two sync directions), `backend/routes/sheets_sync.py` (link management + push endpoint + pull webhook, registered in `app.py`), table `google_sheet_links` (`client_id, spreadsheet_id, link_token, is_notion, client_name, linked_at, linked_by` — `link_token` is the pull webhook's sole authenticator, unguessable, scoped to one client).
    - **Push (Lumina→Sheet):** `applySheetFields()` in `projects.html` now fires a fire-and-forget `POST /api/sheets/push/<task_id>` after every save, same contract as the pre-existing `log-version` call right above it — no-ops server-side for any client with no linked Sheet, so this is safe on every client, not just linked ones.
    - **Pull (Sheet→Lumina):** an Apps Script `onChange` trigger (installed once per Sheet during setup, snippet auto-generated by `POST /api/clients/<id>/google-sheet-link` and shown again by the `GET`) POSTs the Sheet's **entire current contents** to the webhook on every edit/insert/delete. The backend reconciles via a **full-snapshot diff** against that client's current tasks (fetched once per request, not once per row) rather than incremental per-cell events — deliberately, to avoid this codebase's recurring diff-only-payload bug class (gotchas #45/#51/#69). Blank `lumina_task_id` → create; known id missing from the payload entirely → delete; known id with changed fields → update; known id with identical fields → skip.
    - **Loop safety, by construction, not by a lock:** `reconcile_sheet_rows()` only ever writes back to the Sheet for the brand-new-row case (`write_cell` with the freshly created task id) — every other branch writes to Notion/SQLite only, never back to the Sheet. A push-triggered Sheet write does re-fire the Sheet's own `onChange` and lands back at the webhook, but by then the Sheet and Lumina already agree, so that pass's diff for that row is empty and nothing gets written anywhere — no unbounded ping-pong regardless of how precise the equality check is.
    - **Setup, one-time per client:** employee creates a Google Sheet with a 13-column layout (hidden `lumina_task_id` in column A, then the 12 Sheets fields matching `RH_FIELD_LABELS`'s order), shares it with the service account email shown in the "🔗 Google Sheet" modal (new button next to "🕓 Edit History" in the Sheets toolbar), pastes the Sheet's URL into that modal to connect (the backend does a live `read_all_rows` check at connect time so a forgot-to-share mistake surfaces immediately as a connect-time error instead of a silent later push/pull failure), then pastes the returned Apps Script snippet into the Sheet's Apps Script editor and runs `installTrigger()` once.
    - **`assigned_to` is plain-text comma-separated employee display names in the Sheet** (e.g. `Abhinav Gupta, Palak`) — matching exactly what's already stored in Notion's `Assigned To` property, no id-mapping layer like the checkbox-based Lumina UI has. **`Creation Date`/`Post Day` columns must be formatted as Plain Text in the Sheet**, not Date — the Sheets API returns `FORMATTED_VALUE` by default, but a real Date-type cell renders per the *viewer's* locale, not a fixed ISO string, so a typed `YYYY-MM-DD` would round-trip inconsistently. This is a manual setup instruction shown in the modal, not something enforceable in code.
    - **Pre-existing bug discovered (not introduced, not fixed) while writing the Python port of `parseSheetNotes()`:** the frontend's own `parseSheetNotes()` in `projects.html` has stop-lists for the `Content`/`Idea`/`Scripts`/`Caption` regex captures that don't include `Notes` as a boundary (only `Link`'s stop-list does) — so if a row has, say, a filled Content field and a filled Notes field but an *empty* Link field, `Content` greedily captures through to end-of-string and ends up with `"...actual content | Notes: whatever the note was"` appended into it, even though the separate `Notes:` regex still correctly extracts `myNotes` on its own. Confirmed via a Python round-trip test of the new `_parse_sheet_notes`/`_build_sheet_notes` port (which faithfully reproduces this, since it must match the frontend's actual behavior for round-trips to work). Out of scope for this feature — flagged here rather than silently fixed, since it's a pre-existing frontend bug unrelated to what was asked. If a user ever reports a Sheets Content cell containing trailing `"| Notes: ..."` junk, this is the root cause; fixing it means adding `Notes` to the `Content`/`Idea`/`Scripts`/`Caption` stop-lists in **both** `parseSheetNotes()` (`projects.html`) and `_parse_sheet_notes()` (`google_sheets_store.py`) together, or they'll drift.
    - **Verification performed this session:** `pyflakes` clean on both new files and on `app.py`/`db.py` (zero new warnings vs. baseline — the pre-existing `app.py` unused-import noise is untouched by this change). `import app` boots clean after registering the new blueprint. `node --check` clean on all 4 of `projects.html`'s extracted `<script>` blocks. Live `curl` against a running dev server confirmed `POST /api/sheets/push/<id>` no-ops correctly (`{"success":true,"linked":false}`) for a client with no link, and `POST /api/sheets/webhook/<bad-token>` correctly 404s (`{"error":"Unknown link"}`) for an unrecognized token. A Python round-trip test (`_build_sheet_notes` → `_parse_sheet_notes` → `_task_to_fields` → `_fields_to_row` → `_row_to_fields`) confirmed the field-mapping layer is internally consistent.
    - **NOT verified this session, needs a real `GOOGLE_SHEETS_SERVICE_ACCOUNT_JSON` credential:** no live Google Sheet was ever connected, pushed to, or pulled from — the entire push/pull/reconcile path, the Apps Script snippet's actual behavior in a real Sheet, and the "share the sheet with the service account" setup flow are all unexercised against the real Google Sheets API. Before relying on this in production: set the env var, connect a real test client's Sheet, and walk through Task 10 Step 2 of the implementation plan (edit in Lumina → confirm it appears in the Sheet; edit in the Sheet → confirm a new/updated/deleted task appears in Lumina).
    - **Follow-up, same day, after deploying and testing against production (no credential yet, so error-path-only) — found and fixed two real gaps, plus two polish items:**
      1. **`GET`/`DELETE /api/clients/<id>/google-sheet-link` had zero auth**, discovered by testing the deployed routes directly: combined with this app's `CORS(origins="*")`, `GET` leaked the live `link_token` (the pull webhook's sole authenticator) to anyone who knew a `client_id` — and `client_id`s aren't secret, they're readable from other already-unauthenticated endpoints like `/api/notion/dashboard`. Exact same bug class as the pre-existing `/api/auth/clients` gap in security warning #7. Fixed by requiring `_is_admin(user_id)` on both (`user_id` via query string for `GET`, body-or-query for `DELETE`); frontend updated to send it. `POST /api/sheets/push/<task_id>` had the same gap (anyone could push arbitrary fields into a linked client's Sheet) — fixed the same way.
      2. **The "any client, opt-in" scope approved during design was never actually reachable in the UI that shipped.** The connect button had only been wired into the Sheets-tab toolbar, which — per this app's existing gotcha #13 detection — only renders for clients already identified as social media. Non-social clients had no path to the modal at all despite the backend fully supporting them via `is_notion`. Fixed by adding a second entry point in every client card's header (`renderDetail()`), reachable regardless of social-detection status.
      3. **Reconciliation safety guard added:** `reconcile_sheet_rows()` recognized zero known tasks in an empty/garbled snapshot as "the user deleted every row" and deleted them all. A flaky `onChange` firing mid-paste, or the Sheet being momentarily blank during an edit, could have wiped a client's entire task list in one webhook call. Now skips the delete pass entirely (logs a warning, returns `deletes_skipped_safety` instead) whenever a non-empty `current` task set sees zero recognized ids in the incoming snapshot — mass deletion has to go through an explicit action, never an ambiguous payload.
      4. **Linked-status indicator:** new `GET /api/sheets/links` (admin-gated, ids only, no tokens) lets the frontend show a ✓ + green tint on the connect button for clients already linked, loaded once at page init and refreshed after connect/unlink, instead of requiring a click into every client to check.
      - **Verification:** all four changes deployed and confirmed live — auth fix verified via direct `curl` (403 without `user_id`, 200/expected-behavior with it) both from the shell and from a real logged-in browser session (`chrome-devtools` MCP: logged in as an employee, opened the connect modal on a genuinely non-social client (Omotec), submitted a test URL, confirmed via the Network panel that the request correctly reached the backend and got the expected "not configured" 400 — zero console errors). `pyflakes`/`node --check` clean on every edited file. The reconciliation safety guard and the linked-status indicator are code-reviewed and statically verified only — neither has been exercised against a real webhook payload or a real linked client yet, since that still needs the service-account credential.
    - **Second follow-up, same day, requested as an open-ended "improve this further" pass — four more additions, all deployed:**
      1. **`google_sheets_store.config_error()`** distinguishes "not configured at all" from "configured but broken" (invalid JSON, or valid JSON missing `client_email`/`private_key`) — without this, a credential-paste mistake (stray newline, missing outer quote — a realistic failure mode for a multi-line private key pasted into a Railway variable) would have surfaced as "make sure the sheet is shared with ...", sending debugging toward sharing settings that were never the problem. `POST .../google-sheet-link` now uses it. Verified locally: all four states (unset / invalid JSON / missing key / valid) return the correct distinct message.
      2. **Sync-health tracking:** `google_sheet_links` gained `last_push_at`, `last_push_ok`, `last_pull_at`, `last_pull_summary` columns. `push_task_to_sheet()` now returns a bool instead of swallowing its result entirely; the push route and pull webhook both record these after every call. The Connect Google Sheet modal now shows "Last push: <time> ✓/✕" and "Last pull: <time> (created=N, updated=N, ...)" instead of the sync being a black box you can only debug via Railway logs.
      3. **Webhook hardening:** rejects payloads over 1000 rows (`MAX_WEBHOOK_ROWS`) before doing any work, and `reconcile_sheet_rows()` is now called inside a `try/except` in the route (defense-in-depth — no known code path throws uncaught today, but a webhook 500ing with no logged reason would be a bad debugging experience if one ever does).
      4. **Two remaining risks, originally left as documented limitations, fixed on explicit follow-up request ("fix all"):** (a) `write_cell()`'s write-back of a newly created task's id — the one path whose failure could leave a row permanently id-less and get it duplicate-created on the next unrelated edit — now goes through a new `_retry()` helper (3 attempts, short backoff) instead of a single try/except. (b) two rows sharing the same `task_id` within one snapshot (a copy-pasted row where column A wasn't cleared) used to let the later row silently overwrite the first with no trace; `reconcile_sheet_rows()` now detects this via the existing `seen_ids` set, keeps the first occurrence deterministically, and reports it via a new `duplicates` count in the summary (which flows through to `last_pull_summary` in the modal). Both verified with targeted unit tests against mocked dependencies (no live Sheets/Notion calls needed): `_retry()` succeeding after one simulated failure and correctly giving up after exhausting all attempts; a two-row duplicate-id snapshot producing exactly one `_update_task()` call (the first row) and `duplicates: 1` in the returned summary.
      - **Verification:** `pyflakes` clean on `db.py`/`google_sheets_store.py`/`routes/sheets_sync.py`, `node --check` clean on `projects.html`, `import db; db.init_db()` confirms the 4 new columns migrate cleanly against the real `logs/app.db`, `import app` boots clean. Deployed and confirmed live via `curl` (the config-error message now returns correctly instead of the old generic one). Sync-health recording and the row cap are statically verified only — not yet exercised against a real push/pull, same credential blocker as everything else in this gotcha.
    - **Third follow-up, same day, explicitly requested as a "fix every loophole" pass — the most consequential one:**
      1. **Every sensitive route in this feature was gated by `_is_admin(user_id)` where `user_id` came straight from a client-supplied body/query field — not a real session check.** `_is_admin()` is `bool(user_id)` everywhere in this app (deliberate, see gotcha #60), so with this app's `CORS(origins="*")`, literally any external site could POST/GET/DELETE these routes with `user_id: "x"` (any non-empty string, doesn't even need to be a real employee id) and pass. This is the exact same class of gap as the pre-existing `/api/auth/clients` warning (#7), just newly introduced by this feature rather than pre-existing. Fixed by adding `_verified_user_id()` (`routes/sheets_sync.py`), which reads the real `session_token` HttpOnly cookie and calls `routes.auth._verify_session()` — the actual mechanism `app.py::create_client` already uses elsewhere in this codebase, just not previously mirrored here. Applied to `GET/POST/DELETE .../google-sheet-link`, `GET /api/sheets/links`, and `POST /api/sheets/push/<id>`. The pull webhook correctly keeps its link_token-only auth (Apps Script has no browser session to carry a cookie). No frontend changes were needed for this — the browser already sends `session_token` automatically on every same-origin fetch; the `user_id` params the frontend still sends are now vestigial (ignored server-side, harmless to leave).
      2. **`spreadsheet_id` had no format validation** — `_extract_spreadsheet_id()` falls back to the raw input string if the URL regex doesn't match, and that string reaches the Sheets API as a raw path segment. Added `_SPREADSHEET_ID_RE` (alphanumeric + `-`/`_`, 20-100 chars, matching real Google Sheets ids) checked before any API call.
      3. **`is_notion` was trusted from the client body with no server-side sanity check.** Since this route is now genuinely admin-gated, this was never attacker-exploitable, but a mismatch (claiming `is_notion=true` when Notion isn't even configured) would silently misroute every create/update/delete for that client to the wrong backing store. Now rejected at link time if `is_notion=true` and `notion_store.is_configured()` is false.
      4. **`INSERT OR REPLACE` on re-link silently minted a new `link_token` every time**, even when re-connecting the exact same client — invalidating whatever Apps Script trigger was already installed (it POSTs to a URL containing the OLD token) until someone noticed sync had silently stopped and reinstalled the snippet. Now reuses the existing token if the client is already linked, only minting a fresh one on a genuinely first-time connect. (Sync-health columns still reset to NULL on re-link since they're outside the reused-token fix's scope — a cosmetic stats loss, not a correctness issue, judged not worth a full UPSERT rewrite for this.)
      5. **The generated webhook URL trusted `request.host_url`**, which Flask populates from the incoming `Host` header. Added `_base_url()`, which prefers an optional `PUBLIC_BASE_URL` env var (unset by default, so this changes nothing unless explicitly configured) — a forged Host header reaching this app could otherwise make the UI hand a user an Apps Script snippet that POSTs their client's Sheet data to an attacker's domain instead of Lumina's. Whether Railway's edge actually lets a mismatched Host header reach this container is unconfirmed (Railway typically routes by Host, which would likely reject/misroute a forged one before it arrives) — fixed regardless as cheap defense-in-depth, not because a live exploit was confirmed.
      6. **A single malformed row in a webhook payload aborted reconciliation for every other row in the same call.** `reconcile_sheet_rows()`'s per-row body is now wrapped in its own `try/except` (logs and continues, tracked in a new `errored` count in the returned summary) — mirrors the existing per-page isolation already in `notion_store.list_tasks()`/`list_clients()` (gotcha #52), applied here for the same reason: the webhook's only authenticator is the link_token, so a payload that isn't a well-formed Apps Script snapshot has to be tolerated, not trusted.
      7. **No rate limiting on any of the three new POST-ish routes** — this app already has Flask-Limiter wired up (`extensions.py`) and uses it on 2 unrelated routes in `app.py`, but nothing in this feature used it. Added `@limiter.limit(...)`: 10/min on connect (also makes a live external API call), 120/min on push (fires on every Sheets save, can legitimately burst during a paste), 60/min on the webhook.
      - **Verification:** `pyflakes` clean on both edited files, `import app` boots clean. Deployed and confirmed live: logged in via `curl` with real credentials to capture an actual `session_token` cookie, confirmed the OLD bypass (`?user_id=emp003` with no cookie) now correctly 403s, and confirmed the real cookie-authenticated request still succeeds. `_SPREADSHEET_ID_RE` unit-tested locally against a real Sheets id (accepts) and a path-traversal-shaped string (rejects). The `is_notion` cross-check, token-reuse-on-relink, `_base_url()` override, per-row isolation, and rate limits are code-reviewed and statically verified only — none has been exercised against a real linked client yet, same credential blocker as everything else in this gotcha.