[phases.setup]
# Native libs for WeasyPrint (PDF). reportlab fallback works without them but quality is better with WeasyPrint.
nixPkgs = [
  "python311",
  "gcc",
  "libffi",
  "gnumake",
  "cairo",
  "pango",
  "harfbuzz",
  "fontconfig",
  "glib",
]
